# Full-Bytecode Burn-Down Checklist

The finite, closed list of work to reach **full bytecode execution** — every
non-dynamic JS construct running on the production unified-bytecode VM, with the
two interpreter fallback tiers (`ExpressionProgram` tier-1, `ExecutionPlanRunner`
tier-2 IR) retired.

Derived from an exhaustive 6-surface census (206 raw leaf items deduped to 112)
plus an adversarial grammar-completeness audit. Authoritative gate:
`src/Asynkron.JsEngine/Execution/UnifiedBytecode/UnifiedBytecodeProductionEligibility.cs`
(`Evaluate` / `EvaluateScript` / `EvaluateResumable`).

> **Completeness status (from the adversarial audit): Phase 0 has closed the
> known inventory holes.** Constructs that lowering erases (`switch`, `do-while`,
> `debugger`, sequence expr, BigInt, `static {}`, super-in-field-init, ordinary
> `new.target`, labeled non-loop break) are now mapped to grammar coverage,
> compiler-decline leaves, opcode/allowlist gap inventories, or the decomposed
> A35/B24 leaves below. Treat the checklist as the current finite burn-down list;
> future source drift must update the matching audited inventory in the same
> slice.

**Definition of done** — all must hold simultaneously and be machine-checkable:
1. No decline code fires for any non-dynamic shape (only the §Dynamic-Residue set).
2. Sync `Execute` and `ExecuteResumable` admit the same non-dynamic surface (parity).
3. Corpus route census ≈100% of non-residue functions/scripts on production bytecode.
4. Both fallback tiers deleted/quarantined; only a dynamic-scope interpreter remains.
5. A standing CI gate fails if any `isDynamicResidue=false` decline ever fires.
6. The surviving interpreter handles exactly: direct `eval`, awaited-with / live `with` outside the VM-owned current-environment lane, eval-injected bindings, `Function`-constructor bodies. Nothing else.
7. `docs/plans/bytecode-proof-manifest.json` is green under
   `BytecodeProofManifestTests`; closed rows have only admitted/retired proof
   rows, while open rows name exact executable blockers. Final E4/E5b-E5e closure
   requires source absence for legacy AST/expression eval bridges and
   `ExecutionPlanRunner` entry points, followed by a passing suite.

---

## Dynamic Residue — terminal, NOT work (the only permanent fallbacks)

direct eval with runtime-source, multi-arg, spread, or declaration-injecting
bodies · eval-injected runtime bindings · awaited `with` object evaluation and
retained live `with` scopes outside the VM-owned current-environment lane
(**sync and resumable non-awaited `with` are admitted, not residue**) ·
`Function(...)`-produced body.
**NOT residue** (real work, do not mis-park): indirect eval `(0,eval)(s)`, free
*global* identifier reads/calls (admitted via materialized activation), sync
non-awaited `with`, the `Function` call boundary itself. D1/D2/D3 are terminal
dynamic-residue guardrails only, not parking buckets for ordinary compiler,
call-boundary, class/static-block, expression-program, or runner-retirement
work.

---

## Phase 0 — Make the list provably finite (do first; closes the audit gaps)

- [x] **P0.1** Grammar-coverage appendix → `docs/plans/bytecode-grammar-coverage.md` ✅ (covers switch+`let`, do-while, sequence, BigInt literal/arithmetic/`typeof`, `static {}`, super-in-instance/static-fields, `new.target`, labeled-block break — each mapped to its lowering owner + test anchor). **Surfaced 1 real new leaf → A52 (`debugger;`).**
- [x] **P0.2** Enumerate the `UnsupportedPlanShape` compiler umbrellas (A51 / B47 / E2): promoted current `UnifiedBytecodeCompiler.TryCompile` reason templates into named owner leaves A51a1/A51a2, A51b-A51k, A51m, and B47a, with exact source-template drift coverage in `docs/unified-bytecode-expansion-contract.md`.
- [x] **P0.3** Diff `UnifiedBytecodeOpCode` enum vs the sync admit-switch (E3) and the two resumable allowlists; name every enum/instruction-but-not-admitted gap. ✅ (2026-06-06): contract now drift-checks sync prototype opcode guard gaps (6 resumable-only opcodes), resumable opcode allowlist gaps (5 opcodes), and resumable instruction allowlist gaps (0 instruction records) against source.
- [x] **P0.4** Decompose the coarse leaves: split **B24** (class expression) into per-member shapes and **A35** into concrete object-literal member opcode leaves. ✅ (2026-06-05): A35 is now A35a-A35e; B24 is now B24a-B24i; the expansion contract drift-checks both decompositions.
- [x] **P0.5** Delete the dead `LabelControlFlow` enum member + stale contract-doc rows (zero emission sites; labeled loop break/continue already admitted on sync). *(= old E1.)*

---

## Phase A — Synchronous admission surface (81 row-accounted items, by decline code / promoted compiler leaf)

Status: ☐ declined · ◐ partial · ☑ admitted (parity work remains on other engine)

- [ ] **A1** Ordinary captured closures admitted; dynamic activation residue remains in A2/D3 — *CapturedOrDynamicActivation* — captured-closure lane ☑/☑; dynamic/with residue stays open in A2/D3 — `:418` — **Stage 0 done** (commit 1c0b30675): FLAT multi-statement captured closures (counters/accumulators/captured reads+writes, e.g. `function inc(){ n++; return n; }`) now route through the production VM — captured locals lower to dynamic-identifier ops over the threaded closure env. Foundation prerequisite landed first: sync-VM `const` reassignment enforcement (commit d556f5d08) — function/block `const` slots were never marked const, so own- and captured-`const` writes silently succeeded; now they throw TypeError. **Resumable parity (Stage 5) LANDED** (commit 71be17015): generators/async capturing an enclosing local route through `ExecuteResumable` — captured READ + UPDATE (`n++`) alias the same enclosing heap slot across suspension (`UpdateDynamicIdentifier` on the resumable allowlist, 1:1 with the switch); captured-`const` write → TypeError; captured plain/compound/logical STORE now routes through the dynamic-reference stack (proof: UnifiedBytecodeResumableCapturedClosureWriteTests). **Nested-scope unlock landed** (commit 76ebd019d): the `HasOnlyRootFlatSlotMappings` guard was replaced by `HasNoCapturedNameShadowedByNestedScope` — NON-colliding nested-scope closures (loops, catch, nested const/TDZ, multi-level) route. **Option B durable fix landed** (commit cebb626c6): `SlotAssignmentRewriter.TryResolve` no longer mis-stamps a captured name to a shadowing nested-block flat slot, so COLLIDING nested-scope closures now route AND compute correctly too. Captured closures are admitted for sync and resumable flat, nested, and colliding shapes; this row remains open only as the A1/A2 activation-residue umbrella so counts do not hide the still-open dynamic activation lanes below. Ordinary non-residue work must stay in its own rows instead of being parked as A1 activation residue.
- [ ] **A2** Unresolved dynamic activation chain — *CapturedOrDynamicActivation* — sync ◐ / res ☐ — `:418` — rebaselined taxonomy: declaration-free, single-argument literal direct eval is an admitted subset, not open residue. It routes when the function has no captured dynamic activation or retained live `with` object in the closure chain, and sync includes ordinary free dynamic read/store/call plus the bounded caller-activation/implicit-`arguments` reads pinned by `DirectEvalThenOrdinaryDynamicReadStoreCall_UsesUnifiedBytecodeProductionFastPath`, `Evaluate_DirectEvalLiteralExpressionPlan_AcceptsExecutableInvocationBoundary`, `Evaluate_DirectEvalLiteralReadsArgumentsAndActivationBinding_AcceptsExecutableInvocationBoundary`, `DirectEvalIdentifierCall_UsesCallerEnvironmentOnProductionFastPath`, `DirectEvalArgumentsAndActivationRead_UsesCallerEnvironmentOnProductionFastPath`, manifest `A2-sync-declaration-free-direct-eval-routes`, and manifest `A2-sync-bounded-arguments-direct-eval-routes`. Resumable declaration-free literal direct eval is pinned separately for generator, async, and async-generator routes by manifest `A2-resumable-generator-declaration-free-direct-eval-routes`, `A2-resumable-async-declaration-free-direct-eval-routes`, and `A2-resumable-async-generator-declaration-free-direct-eval-settles`, while resumable generator, async, and async-generator arguments-dependent direct-eval activation remain open via `A2-resumable-generator-arguments-direct-eval-boundary-stays-open`, `A2-resumable-async-arguments-direct-eval-boundary-stays-open`, and `A2-resumable-async-generator-arguments-direct-eval-boundary-stays-open`; the shared dynamic-activation detector remains pinned by `A2-resumable-dynamic-activation-direct-eval-boundary-stays-open`. D1/D2 remain closed terminal-residue gates: multi-arg/spread direct eval, identifier-loaded/runtime-source eval, and declaration-bearing eval literals intentionally decline to IR while computing correctly; eval-injected runtime bindings belong to D2 quarantine unless a future slice explicitly owns direct-eval declaration instantiation in the VM. Retained live-`with` closure chains are pinned by `ClosureWithRetainedWithObjectRead_DeclinesProductionRoute_ButRunsCorrectly`, `ClosureWithRetainedWithObjectReceiverCall_DeclinesProductionRoute_ButRunsCorrectly`, and manifest `A2-retained-with-closure-read-stays-open` / `A2-retained-with-closure-receiver-call-stays-open`: they decline because the VM owns active current-environment `with` lookup/receiver semantics, not closure-retained with-object environment ownership. Remaining A2 gaps: captured dynamic activation chains beyond the admitted captured-closure lane, arguments-object-dependent direct-eval shapes outside the owned bounded arguments route, and resumable parity where `TypedAstEvaluator.UnifiedBytecodeResumableActivation` still treats arguments/dynamic direct-eval dependencies as unowned. A2 is not a holding row for ordinary call/member/super/private call boundaries, A51 compiler leaves, B24h/B36 class/static-block work, E4 expression-program retirement, or E5 runner retirement.
- [x] **A3** `arguments` whole-object dependency (escape/pass/return/mutate) — *ArgumentsObjectDependency* — ☑/☐ — `:427` ✅ (Codex)
- [x] **A4** `arguments` as call target — *ArgumentsObjectDependency* — ☑/☐ — `:1172` ✅ (Codex)
- [x] **A5** `arguments` store / assignment-reference — *ArgumentsObjectDependency* — ☑/☐ — `:1340` ✅ (Codex)
- [x] **A6** Arrow needing lexical-this / new.target environment (non-simple body) — *ArrowLexicalThisDependency* — ◐/n/a — `:455` ✅ (commit 3591e8507): FLAT multi-statement arrow bodies now route through the production VM (lexical `this`/`new.target` threaded before VM entry, body-shape-agnostic). Mirrors closure Stage 0: lifted the arrow path off `SimpleReturnProgram`-only. **Nested-scope unlock landed** (commit 76ebd019d): now bounded by `HasNoCapturedNameShadowedByNestedScope` — NON-colliding nested-scope arrows route. **Option B durable fix landed** (commit cebb626c6): `SlotAssignmentRewriter.TryResolve` no longer mis-stamps a captured name to a shadowing nested-block flat slot, so COLLIDING nested-scope arrows now route AND compute correctly too. Fully admitted for flat + nested + colliding shapes.
- [x] **A7** Class constructor activation outside admitted param shapes — *ClassConstructorActivation* — ☑/n/a — `:462` ✅ (Codex): BASE-class ctors with general multi-statement and nested-lexical-scope bodies route through production bytecode; derived ctors with `super(...)`, instance-field-initializer ctors, and named/computed super-property call targets route and preserve `this`-before-`super` TDZ plus field order. Private-name constructor activation now routes too: the production constructor bridge initializes private brands/fields and enters own/captured private-name scopes before VM execution, so base/derived private-brand-only constructors, direct private writes such as `this.#p = v`, and nested private-read RHS values such as `this.q = this.#p + 1` hit `unified-bytecode-production-fast-path func=C`.
- [x] **A8** Tail call returned from inside `finally` — *CallDependency* — ☑/n/a — `:498` ✅ (commit 5221fb042): SLOPPY finally-return calls now route (never TCO'd anywhere, so the no-TCO VM is no worse); STRICT finally-return calls stay declined (the IR runner TCO-restarts only strict same-function tail calls — verified 100k-deep, no StackOverflow). Gated the over-broad `ContainsCallReturnReachableFromFinally` decline on `isStrict`, mirroring the A9/A10 strict-tail-call boundary. Residual: sloppy free-identifier callee living only in the finally region (finally-edge dynamic-name gating gap).
- [x] **A9** Identifier call-target outside first invocation boundary — *CallDependency* — ☑/◐ — `:1204` ✅ (commit 26bc64504): sequential identifier calls (`a(); return b();`) route once the dynamic call-target path is enabled; eval order preserved.
- [x] **A10** Free identifier call target (`helper(x)`) — sync only; resumable admits — *DynamicLookupDependency* — ☑/☑ — `:1195` ✅ (commit 26bc64504): free/global call targets route via `HasOrdinaryDynamicCallTargetDependency` (dynamic-name path, mirroring resumable). `this` = undefined receiver, unbound → ReferenceError. **TCO boundary:** STRICT tail-position `return <identifier-call>` is DECLINED (kept on the TCO-capable IR runner) since the production VM has no tail-call optimization — prevents StackOverflow on strict self-recursion (`ContainsTailPositionIdentifierCallReturn`). Non-strict tail calls + statement/argument calls stay admitted. (The per-iteration-const-continue decline guard that this originally added was REMOVED in commit ddad73e6a — the underlying VM TDZ bug was fixed via `MirrorDynamicLexicalToFlatSlot`, so the shape now routes through production correctly instead of declining.)
- [x] **A11** Complex call arguments outside admitted spans (`fn(a+g(b))`) — *CallInvocationBoundary* — ☑/◐ — `:1245` ✅ (commit ee4506026): general operand-stack region walker (eligibility `TryValidateAdmittedComplexCallArgumentRegion` + compiler `TryAppendAdmittedComplexCallArgumentRegion`) admits args built from any already-admitted value op — nested calls `g(h(x))`, call-in-binary `g(a+h(b))`, member/computed-member call args. Left-to-right eval order proven. Assignment args, spread, out-of-vocabulary ops correctly still decline (region rolls back).
- [x] **A12** Member/computed call-target outside first boundary (`a.b().c()`) — *CallInvocationBoundary/CallDependency* — ☑/◐ — `:1240` ✅ (commit ef1a620bb): chained method/computed call targets (`a.b().c()`, `o.a()[k]()`) route — generalized the named-member candidate to the canonical operand-stack model (named+computed reads, call-target prep, Call boundaries); the compiler lowers the whole chain in source order, each inner call's result becomes the next receiver, final call `this` = immediate receiver. L-to-R eval order proven. Strict self-recursive member tail call unchanged (no TCO regression).
- [x] **A13** Free identifier READ (sync only; resumable admits) — *DynamicLookupDependency* — ☑/☑ — `:1328` ✅ (commit af1be8ecc): already admitted via `allowsOrdinaryDynamicIdentifiers` → `LoadDynamicIdentifier`; locked with adversarial tests (global value; undeclared → ReferenceError).
- [x] **A14** Free dynamic identifier STORE `freeVar = x` (both routes) — *DynamicLookupDependency* — ☑/☑ — `:1359` ✅ (commit af1be8ecc): admitted; dynamic writes route through the explicit assignment-reference pair, creating configurable sloppy globals while strict unresolvable writes throw `ReferenceError`. Both verified.
- [x] **A15** `typeof freeName` dynamic lookup — *DynamicLookupDependency* — ☑/☑ — `:1417` ✅ (commit af1be8ecc): `TypeOfDynamicIdentifier` → "undefined" for unbound (never throws), correct type for bound.
- [x] **A16** Computed-property delete key `delete box[freeName]` — *DynamicLookupDependency* — ☑/◐ — `:1632` ✅ (commit af1be8ecc): free key lowers via `LoadDynamicIdentifier`; first-boundary computed-delete candidate accepts.
- [x] **A17** Named property read past first boundary (`box.a.b.c.d`) — *PropertyReadBoundaryOutOfScope* — ◐/◐ — `:1652` ✅ #3154
- [x] **A18** Computed property read past first boundary (`box[a][b][c]`) — *PropertyReadBoundaryOutOfScope* — ◐/◐ — `:1785` ✅ #3154
- [x] **A19** Named/computed write past admitted (`box.a.b.c=v`, `a=b=v`) — *PropertyWriteDependency* — ◐/◐ — `:1812` ✅ #3160
- [x] **A20** Ternary/branching computed-key write `box.child[c?a:b]=v` (+ update/delete) — *PropertyWriteDependency* — ☑/◐ ✅ #3121
- [x] **A21** Compound/logical computed write w/ ternary key `box[c?a:b]+=v`, `a&&b`, `a??b` — *PropertyWriteDependency* — ☑/◐ ✅ #3121
- [x] **A22** Identifier update on free name `freeName++` — *PropertyUpdateDependency* — ☑/☑ — `:1829` ✅ (commit af1be8ecc): `UpdateDynamicIdentifier` returns old/new and mutates the global; verified.
- [x] **A23** Update w/ computed receiver prefix `box[k1].child[k2]++` — *PropertyUpdateDependency* — ◐/◐ — `:1850` ✅ #3168
- [x] **A24** `delete freeName` — *DeleteDependency* — ☑/☑ — `:1861` ✅ (commit af1be8ecc): `DeleteDynamicIdentifier` — correct false/true per configurability, unresolvable-ref → true.
- [x] **A25** Named property delete past admitted (`delete box.a.b.c`) — *DeleteDependency* — ◐/◐ — `:1888` ✅ #3155
- [x] **A26** Computed property delete past admitted (`delete box[k1][k2]`) — *DeleteDependency* — ◐/◐ — `:1918` ✅ #3155
- [x] **A27** `super.m()` / `super[k]()` call-target prep outside first boundary — *SuperPropertyDependency* — ◐/☑ — `:1302` ✅ (commit 6e5016977): admitted named/computed super call-target preparation through the proven first-boundary/member-call and complex-RHS spans; verified by eligibility op-stream coverage plus constructor route tests for `super.m()` and `super[name]()` receiver preservation.
- [x] **A28** Optional-chain named read beyond single hop `a?.b?.c` — *OptionalChainDependency* — ◐/◐ — `:1464` ✅ #3161
- [x] **A29** Optional-chain computed read beyond admitted `a?.[k]?.[j]` — *OptionalChainDependency* — ◐/◐ — `:1689` ✅ #3162
- [x] **A30** Optional member/computed call beyond admitted `a?.b?.c()`, `o?.[k]()` — *OptionalChainDependency* — ◐/◐ — `:1232` ✅ #3165
- [x] **A31** Optional short-circuit guard outside admitted spans — *OptionalChainDependency* — ◐/☐ — `:1992` ✅ (commit 7bb14bfbb): standalone multi-hop optional named/computed reads route; named and computed second-hop call arguments route (`fn(box?.value?.nested)`, `fn(box?.child?.value)`, `fn(box?.[key]?.[key])`, `fn(box?.[key]?.value)`, `fn(box?.prop?.[key])`). The remaining documented concrete `OptionalChainDependency` sync delete lane is now covered by A32; resumable-only optional-computed-hop calls remain a plan-walk decline.
- [x] **A32** Optional-chain delete chained `delete a?.b?.c` / `delete a?.[k1][k2]` — *OptionalChainDependency* — ◐/◐ — `:1619` ✅ (Codex): optional-delete shapes now route through production unified bytecode for terminal optional named deletes (`delete box?.value?.leaf`), non-terminal optional named deletes (`delete box?.value.leaf`), terminal optional computed deletes (`delete box?.[key]`, `delete box?.child?.[key]`), and optional computed-read receiver plus terminal computed delete (`delete box?.[first][second]`). Nullish short-circuit returns `true` without evaluating later key spans; present-path deletes and null-intermediate TypeErrors stay VM-owned.
- [x] **A33** Array spread non-simple source `[...f().items]`, `[...gen()]` — *ObjectLiteralOrSpreadDependency* — ◐/◐ — `:2014` ✅ #3166
- [x] **A34** Object spread non-simple source `{...f()}` — *ObjectLiteralOrSpreadDependency* — ◐/◐ — `:2036` ✅ #3167
- [x] **A35a** Computed data property object literal (`DefineComputedObjectProperty`) — *ObjectLiteralOrSpreadDependency* — ☑/☑ — `:2019` ✅ computed keys/values route in sync and resumable literal spans; non-admitted key/value sub-shapes stay owned by their underlying dependency rows.
- [x] **A35b** Static shorthand method object literal (`DefineObjectMethod`) — *ObjectLiteralOrSpreadDependency* — ☑/☑ — `:2019` ✅ sync route pinned; resumable route now admits non-capturing method function literals through `LoadFunctionLiteral` + `DefineObjectMethod`.
- [x] **A35c** Computed shorthand method object literal (`DefineComputedObjectMethod`) — *ObjectLiteralOrSpreadDependency* — ☑/☑ — `:2019` ✅ sync route pinned with computed-key order; resumable route now admits non-capturing computed method literals.
- [x] **A35d** Static accessor object literal (`DefineObjectAccessor`) — *ObjectLiteralOrSpreadDependency* — ☑/☑ — `:2019` ✅ sync route pinned; resumable route now admits non-capturing getter/setter literals.
- [x] **A35e** Computed accessor object literal (`DefineComputedObjectAccessor`) — *ObjectLiteralOrSpreadDependency* — ☑/☑ — `:2019` ✅ sync route pinned; resumable route now admits non-capturing computed getter/setter literals.
- [x] **A36** Private-field define in object literal `{#x:v}` — *PrivateFieldDependency* — n/a — `:2042` ✅ (commit 45aea8402): RESOLVED — `{#x:v}` is not a valid shape; the parser previously (wrongly) accepted it as a property named `"#x"`. Now `ParseObjectPropertyKey` throws a SyntaxError (private names are class-body-only), matching spec. Nothing to admit; the decline path is unreachable for valid input.
- [x] **A37** Private-named mutation outside admitted direct shapes — *PrivateFieldDependency* — ☑/☑ — `:1093` ✅: private-field mutation is admitted for plain write `this.#x=v`, `++`/`--`, simple compound `+=`, cross-instance `other.#x=v`, complex-RHS `this.#x=a*b+c`, private method call `this.#m()`, and private-read RHS mutation shapes such as `this.#x = this.#x + a` / `this.#x += this.#x`. Remaining private-adjacent work is no longer A37 mutation; it stays in A51f5/A51g/A51i for optional/private call-neighbor, receiver-neighbor mutation, super-neighbor, and source-backed compiler helper diagnostics.
- [x] **A38** for-in unsupported driver source (awaited / non-lowered) — *ForInDriverStateDependency* — ☑/☑ — `:524` ✅ (Codex)
- [x] **A39** Array destructuring unsupported driver — *DestructuringDependency* — ☑/☐ — `:531` ✅ (Codex)
- [x] **A40** Object destructuring unsupported driver — *DestructuringDependency* — ☑/☐ — `:544` ✅ (Codex)
- [x] **A41** Slot-resolved identifier via dynamic-name reference op — *UnsupportedPlanShape* — ☑/☑ — `:1346` ✅: explicit slot-resolved consumed identifier assignments now route through production unified bytecode. `ResolveIdentifierReference` tracks the pending slot reference in the compiler/eligibility side-state and `StoreResolvedIdentifier` lowers to `DuplicateTop` + `StoreSlot`, preserving the assignment value for `return (x=v)`, `var y=(x=v)`, `g(x=v)`, and chained `(x=z=5)`. Name-only activation-slot matches still decline because dynamic/`with` shadowing cannot be proven by name alone.
- [x] **A42** `using` / `await using` declaration *(split → P0.4)* — *UnsupportedPlanShape* — ☑/☑ — `:571` — ✅ sync `using` ROUTED: production unified bytecode now emits `RegisterDisposable` after initializer evaluation and before flat-slot/binding-target storage, so admitted sync function-local `using` declarations register resources on the active environment and dispose on normal/throw completion or block `PopEnvironment`. Direct function-body sync `using` declarations in generator/async bodies now route through `ExecuteResumable` too: the invokers materialize the body environment for disposal ownership, the VM handles `RegisterDisposable`, and completed/throwing resumable steps dispose before surfacing the final result. Null/undefined stays a no-op and non-object values throw `TypeError`. Block-scoped resumable `using` remains declined until the resumable VM owns a persisted environment stack; `await using` remains explicitly declined until async-dispose promise settlement is VM-owned. Separately, a real disposal correctness bug was fixed earlier (commit 5e14e701): function-body-scope `using` never fired `[Symbol.dispose]` because the function environment is never popped via `PopEnvironment`; `RunSync`/`ExecuteAsyncStep` now dispose on completion+throw (LIFO, SuppressedError preserved). Known residual: `await using` async-dispose promise isn't awaited before resolution (pre-existing engine-wide limitation).
- [x] **A43** Descriptor-backed block-scoped function declaration (Annex B) — *UnsupportedPlanShape* — ☑/☑ — `:581` — ✅ ROUTED (Codex): descriptor-backed block function declarations now force a flat-slot owner for the active block binding during slot rewriting, so `PushEnvironment` can materialize the strict block scope and `DeclareFunction` can run the existing Annex B update path without dropping the block binding. The blanket descriptor decline is removed; descriptor functions that capture activation bindings still decline until the VM owns that materialized closure shape. Focused proof covers ordinary blocks, synthetic if-branch blocks, sloppy Annex B var updates, the strict no-leak tripwire, and the full `UnifiedBytecodeProductionEligibilityTests` class.
- [x] **A44** PushEnvironment for iterating / non-flat-slot lexical block (per-iter `let`) — *UnsupportedPlanShape* — ☑/☑ — `:599` ✅ STAGE 1 ADMITTED: captured per-iteration `let`/`const` loop bindings now route through production unified bytecode. `SlotAssignmentRewriter` eagerly maps PushEnvironment lexical slots and shares the loop-head/per-iteration flat slot for copy-listed bindings; the compiled `UnifiedBytecodeScopeDescriptor` carries per-iteration copy slot metadata; the VM snapshots those slots before PushEnvironment rebinding and initializes the fresh scope environment instead of TDZ-wiping copied slots. Pinned by `A44PerIterationLetDeclineTests` route assertions plus loop/block scope regression guards. A43 Annex-B block-function admission is also routed; B23 resumable nested-function capture remains separate foundation work.
- [x] **A45** with-depth analysis failure (unbalanced Enter/Leave, irreducible flow) — *UnsupportedPlanShape* — ☑/☑ — `:482` ✅ (Codex): `UnifiedBytecodeWithDepthAnalysis` now propagates active with-depth through `EnterTryInstruction` catch/finally region entries, and the ordinary dynamic-name scan has a separate zero-depth exception-region pass for read-only free names, free `typeof` names, and free call targets. This admits finally-return free callees, catch-region free reads/`typeof`, and try/catch/finally under active `with` without weakening invalid-target, inconsistent-depth, catch-only free-store/update/delete/reference, or A51c-style scope/environment declines.
- [x] **A46** Non-production binary operator (`**`, BigInt-mixed, …) — *UnsupportedPlanShape* — ☑/◐ — `:2061` ✅ (commit 9058879 + c4f4683e9): `**` on Numbers admitted; the ENTIRE pure-BigInt binary operator + comparison surface (`+ - * / % ** << >> & | ^`, relational/equality) routes through the sync VM (the earlier 'declines' was a `.toString()` method-call artifact). BigInt-mixed (`1n+1`) routes and throws the correct TypeError. Pinned. Also fixed `-2**2` SyntaxError.
- [x] **A47** for-of unsupported iterator-init source — *UnsupportedPlanShape* — ☑/☑ — `:517` ✅ (Codex)
- [x] **A48** Sync iterator driver: async iterator kind — *UnsupportedPlanShape* — ☑/☑ — `:2332` ✅ (Codex): stale row closed by current source proof. `IsSupportedIteratorInit` now accepts `IteratorDriverKind.Await` when exactly one source payload is present, and the resumable VM owns async iterator `next()` result/value awaits plus close settlement. Focused anchors: `IsSupportedIteratorInit_AsyncKind_Accepts`, `IsSupportedIteratorInit_AsyncKindWithAwaitedSource_Accepts`, and `EvaluateResumable_ForAwaitOfDriver_AdmitsAsyncIteratorDriver`.
- [x] **A49** Plan with no ActivationSlots metadata — *UnsupportedPlanShape* — ☑/☑ — `:204` ✅ (commit c4f4683e9): RESOLVED — the `Activation slot metadata is required` decline arm is UNREACHABLE for any valid compiled plan (`BuildActivationSlotShape` is unconditional; nested-fn restamp carries it forward). A pure defensive backstop; trivial plans pinned.
- [x] **A50** Default prototype-only opcode guard (drift backstop) *(→ P0.3)* — *UnsupportedPlanShape* — n/a/n/a — ✅ decomposed by P0.3: every declared opcode not explicitly admitted by `TryFindPrototypeOnlyOpcode` is now named and drift-checked under `Sync Prototype Opcode Guard Gaps`; the only current gaps are resumable-only suspension opcodes.
- [x] **A51** Compiler `TryCompile` failure umbrella *(decomposed by P0.2)* — *UnsupportedPlanShape* — see A51a1/A51a2, A51b-A51k, A51m, and B47a below.
- [x] **A51a1** Compiler malformed entrypoint/target integrity guards — owner: `UnifiedBytecodeCompiler.cs` and `UnifiedBytecodeProductionEligibility.cs`; fallback route: none for valid generated plans; sync/resumable: both. The `Unsupported entrypoint.` arm is pinned as unreachable for valid compiler-produced function, script, and resumable plans; it remains only as malformed-plan protection in the compiler, production eligibility, and with-depth analysis. The invalid-target guard likewise protects corrupted/manual control-flow maps after compilation and is not a generated JavaScript source gap.
- [ ] **A51a2** Compiler loop-control and statement-topology diagnostics — owner: statement/control-flow lowering in `UnifiedBytecodeCompiler.cs`, with compact statement-shape ownership in `StatementInstructionDiagnosticsCodec.cs`; fallback route: existing execution-plan runner; sync/resumable: both. Switch-style `BreakableKind.HandlesCompletionInternally` wrappers are now sync- and resumable-admitted; the compiler already erases the marker into VM-owned branch targets, so switch bodies with `yield`, `break`, `return`, and `default` no longer decline solely on `BreakableEnterInstruction`. Remaining source-backed `Unsupported loop control flow at instruction ...` diagnostics stay open until the compiler proves the generated topology and VM cleanup route.
- [ ] **A51b** Activation-slot metadata, slot-layout, unsupported declaration / assignment / update / storage targets, and iterator/for-in TDZ-head flat-slot mapping residue — owner: slot-layout and flat-storage lowering; fallback route: existing execution-plan runner; sync/resumable: both. Manifest row `A51b` now pins the open compiler/eligibility source anchors for activation-slot metadata plus awaited declaration, declaration, assignment, compound-assignment, update target storage guards, and the iterator/for-in TDZ-head flat-slot mapping residue. Partial: unresolved non-dynamic `IncrementSlotInstruction` compiler failures now report `Unsupported update target '{incrementTargetSymbol.Name}'.`, keeping update-target storage gaps in the A51b bucket instead of the generic unsupported-instruction bucket. The iterator/for-in TDZ-head decline `Iterator/for-in driver TDZ head binding '{binding.Name}' could not be resolved to a flat activation slot.` is pinned by the proof manifest as A51b flat-slot mapping residue.
- [ ] **A51c** Catch binding, lexical dynamic declaration, active-with dynamic-name, and non-flat scope/environment TDZ-head binding storage gaps — owner: scope/environment lowering; fallback route: existing environment-aware execution-plan runner; sync/resumable: both. Manifest row `A51c` now pins the non-simple/destructuring catch binding topology guard, unsupported catch binding slot mapping guard, lexical dynamic declaration storage through `DeclareDynamicLexical`, active-with dynamic-name gates, materialized-activation dynamic residue, and the ordinary dynamic-name activation-slot boundary. This keeps ordinary catch and TDZ storage as non-residue scope/environment work while preserving active-`with` dynamic-name ownership as dynamic residue.
- [x] **A51d** Iterator, for-in, `yield*`, resume-target, and driver state-slot gaps — owner: iterator/resume-state lowering; fallback route: existing iterator/generator/async IR drivers; sync/resumable: both, with `yield*` and resume-target failures resumable-owned. ✅ (Codex): current generated plans route through the synthetic slot layout for iterator/for-in drivers, awaited-source lanes, async iterators, `yield*`, and resume-target storage. The remaining compiler reason templates are defensive malformed/manual-plan diagnostics, not known generated-code gaps; focused ratchets pin generated for-in routing plus malformed for-in and `yield*` state-slot declines before VM entry.
- [x] **A51e** Array/object destructuring state-slot and target gaps — owner: destructuring driver lowering; fallback route: existing destructuring IR helpers; sync/resumable: both. ✅ (Codex): stale compiler leaf closed for the intended simple script declaration lane. `AddSyntheticDestructuringStateSlots` allocates VM scratch slots for array/object driver state, and `TryResolveDestructuringTarget` emits dynamic script targets for top-level `var` / `let` / `const` simple array/object destructuring. Broader nested/default/parameter/disposal destructuring remains tracked under R6 / `DestructuringDependency`.
- [x] **A51f** General expression-loop unsupported op, binding-target expression, dynamic identifier, `arguments`, and private-neighbor umbrella *(decomposed by A51f build slice)* — *UnsupportedPlanShape* — see A51f1-A51f5 below.
- [x] **A51f1** General expression-loop unsupported op catch-all — owner: expression-program lowering; sync/resumable: both. ✅ (Codex): source-ratchet proof now compares every declared `ExpressionOpKind` to direct cases in `TryAppendExpressionProgramOps`; there are zero missing general-loop opcode cases. The remaining `Unsupported expression op '{operation.Kind}'.` switch default is a defensive malformed/manual-op guard. Helper diagnostics for unsupported simple operands, literal control spans, and property-read bases are real narrower boundaries owned by A51h/A51i/A51k, not a separate catch-all row.
- [x] **A51f2** Binding-target expression context gap — owner: expression-program lowering / binding-target program bridge; sync/resumable: both. ✅ (Codex): generated expression-program compiler contexts now carry binding-target descriptor constants, including `TryCompileStandaloneExpressionProgram`, so assignment destructuring lowered through `ApplyBindingTarget` compiles to standalone unified bytecode instead of declining on missing binding-target context. The remaining `Binding-target expressions are not available in this unified bytecode compilation context.` diagnostic is a defensive helper guard for malformed/manual calls without a descriptor builder, not a known generated production fallback.
- [ ] **A51f3** Dynamic identifier expression-loop gaps — owner: expression-program dynamic-name lowering in `UnifiedBytecodeCompiler.cs`, ordinary dynamic-name dependency gating in `UnifiedBytecodeProductionEligibility.cs`, and dynamic reference/update/delete VM ops in `UnifiedBytecodeVirtualMachine.cs`; fallback route: existing environment-aware expression-program / execution-plan runner; sync/resumable: both. Partial: zero-depth catch/finally-region free identifier reads, `typeof` free identifiers, and plain statement free stores now enable the ordinary dynamic-name route through `LoadDynamicIdentifier`, `TypeOfDynamicIdentifier`, `ResolveDynamicIdentifierReference`, and `StoreDynamicIdentifierReference`, matching the existing free-call-target exception-region scan. The ordinary expression loop already owns dynamic assignment references, updates, compound/logical writes, and deletes once that route is enabled; the still-open A51f3 boundary is using consumed assignment references, updates, compound/logical writes, or deletes as zero-depth catch/finally route-enabling evidence, plus lexical dynamic declarations, active-with-only shapes, and materialized-activation dynamic-name ownership.
- [x] **A51f4** Implicit `arguments` expression-loop gaps — owner: expression-program `arguments` lowering; fallback route: existing arguments-object / execution-plan runner for broader optional-chain and unbounded arguments-object shapes; sync/resumable: sync production closed for this leaf. ✅ (Codex): implicit `arguments` assignment references now lower through `ResolveDynamicIdentifierReference` / `StoreDynamicIdentifierReference`, including expression-result assignments (`return (arguments = 1)`) and complex call arguments (`reader(arguments = 7)`). Nested implicit `arguments()` call targets inside complex call arguments lower through `PrepareDynamicIdentifierCallTarget`, and the stale `arguments typeof` compiler reason is gone; ordinary `typeof arguments` already emitted the bounded object/dynamic-type path. Parameter or lexical `arguments` bindings remain activation-slot cases. Optional-call shapes such as `arguments?.()` still decline under the generic optional-chain/call-target boundary, not this arguments-specific compiler leaf.
- [ ] **A51f5** Private-neighbor expression-loop gaps — owner: expression-program private-name lowering; fallback route: existing private-name-aware expression evaluation; sync/resumable: both. Partial: plain private named reads are now admitted as simple value operands inside binary/RHS spans, including `receiver.#field + delta`, `this.#x = this.#x + a`, `this.#x += this.#x`, and private-read class-constructor RHS stores. Single-hop optional private reads (`receiver?.#field`) also route through `GetNamedPropertyOptional`. Private receiver-prefix named calls such as `receiver.#child.value()` now route by lowering the private prefix read before `PrepareNamedCallTarget`, receiver-prefix public named/computed writes (`receiver.#child.value = value`, `receiver.#child[key] = value`) now route by composing a single non-optional private receiver read with `SetNamedProperty` / `SetComputedProperty`, and the matching one-hop public named/computed updates (`receiver.#child.value++`, `receiver.#child[key]++`) now route through `UpdateNamedProperty` / `UpdateComputedProperty`. Remaining source-backed diagnostics cover private named delete defense/manual-plan diagnostics, chained optional-private receiver-neighbor reads, deeper private-prefix update chains, private receiver-neighbor compound/logical/private-terminal writes, and private super/member call-neighbor boundaries.
- [x] **A51g** Call-target preparation, direct-eval boundary, member/super/private call-target, and invocation-boundary shape gaps *(decomposed by A51g build slice)* — owner: call-boundary lowering; fallback route: existing call/eval/super IR paths; sync/resumable: both; see A51g1-A51g4 below. Direct private named method calls now route as both primary calls and nested complex call arguments through `PrepareNamedCallTarget` plus `CallInvocationBoundary`; private receiver-prefix named calls such as `receiver.#child.value()` also route after the private prefix read is lowered. Remaining private-adjacent call-target work is outside those named-method and private-prefix named-call shapes.
- [ ] **A51g1** Direct-eval call-boundary metadata and same-engine invocation quarantine — owner: `UnifiedBytecodeCompiler.cs`, `UnifiedBytecodeProductionEligibility.cs`, `UnifiedBytecodeVirtualMachine.cs`; fallback route: existing eval IR path; sync/resumable: both. Proof anchors: call-boundary operand packing and direct-eval bit handling in `EncodeCallBoundaryOperand` / `CallInvocationBoundary`, eligibility direct-eval decliners such as `Evaluate_DirectEvalMultipleArguments_DeclinesProductionRoute`, `Evaluate_DirectEvalSpreadArguments_DeclinesProductionRoute`, `Evaluate_DirectEvalIdentifierCallOpOutsideAdmittedShape_DeclinesAsCallDependency`, and admitted literal anchors `Evaluate_DirectEvalLiteralExpressionPlan_AcceptsExecutableInvocationBoundary` / `Evaluate_DirectEvalLiteralReadsArgumentsAndActivationBinding_AcceptsExecutableInvocationBoundary`. Excludes D1/D4 terminal dynamic residue: multi-argument/spread/runtime-source/declaration-bearing eval and `Function(...)` produced bodies remain quarantine rows unless a future slice explicitly owns that boundary.
- [ ] **A51g2** Private-adjacent call-target preparation residue — owner: `UnifiedBytecodeCompiler.cs`, `UnifiedBytecodeProductionEligibility.cs`, `UnifiedBytecodeVirtualMachine.cs`, with focused proof in `UnifiedBytecodeProductionEligibilityTests.cs`; fallback route: existing private-name-aware call evaluation; sync/resumable: both. Proof anchors: `TryAppendFirstBoundaryCallTargetPreparation`, `TryIsFirstBoundaryCallTargetPreparationCandidate`, `PrepareNamedCallTarget` / `CallInvocationBoundary`, and the already-routed `Evaluate_PrivateReceiverPrefixNamedCall_AcceptsCallTargetPreparation`, `Evaluate_PrivateNamedMethodCall_AcceptsCallTargetPreparation`, and `Evaluate_PrivateNamedMethodCallArgument_AcceptsNestedCallTargetPreparation` tests. Excludes direct private named calls and private receiver-prefix named calls already routed above; private read/property/update/delete neighbor work stays in A51f5/A51i/A51j.
- [ ] **A51g3** Member and super call-target preparation residue — owner: `UnifiedBytecodeCompiler.cs`, `UnifiedBytecodeProductionEligibility.cs`, `UnifiedBytecodeVirtualMachine.cs`; fallback route: existing member/super IR paths; sync/resumable: both. Proof anchors: `TryAppendFirstBoundaryCallTargetPreparation`, `PrepareNamedSuperCallTarget` / `PrepareComputedSuperCallTarget`, VM `PrepareNamedSuperCallTarget`, and tests `Evaluate_NamedSuperCall_AcceptsSuperCallTargetPreparation`, `Evaluate_ConstructorSuperPropertyCalls_AcceptSuperCallTargetPreparation`, `Evaluate_SuperCall_AcceptsSuperConstructInvocationBoundary`, and spread/simple-argument super construct anchors. Excludes already-closed A12 member/computed call chains, A27 `super.m()` / `super[k]()` call-target prep, and A30 optional member/computed call work unless a source-backed call-target diagnostic remains outside those rows.
- [ ] **A51g4** Nested call-argument and invocation-boundary shape diagnostics — owner: `UnifiedBytecodeCompiler.cs` and `UnifiedBytecodeProductionEligibility.cs`; fallback route: existing execution-plan runner; sync/resumable: both. Proof anchors: `TryAppendAdmittedComplexCallArgumentRegion`, `TryValidateAdmittedComplexCallArgumentRegion`, `CallInvocationBoundary`, nested call tests that assert call-boundary op streams, and current compiler diagnostics such as `Unsupported nested call in complex call argument.` This row excludes A11's already-admitted general complex call-argument region and D1/D4 direct-eval/function dynamic residue; it should only own source-backed invocation-boundary shapes that cannot be assigned to a narrower call-target child.
- [ ] **A51h** Method/accessor and control-expression literal/span gaps — owner: literal/span lowering; fallback route: existing expression-program literal/spread evaluation; sync/resumable: both. Manifest row `A51h` now pins the non-container residue anchors for method/accessor/private/name-inference object-literal restrictions plus literal-span logical-control and conditional consequent/alternate diagnostics. Partial: object-literal method/accessor members inside a first-boundary object-literal RHS span now mirror eligibility and compiler emission, including a trailing spread after a method member; broader literal/span compiler leaves remain open.
- [ ] **A51h1** Spread-source and computed-object-key literal-span gaps — owner: literal/span lowering in `UnifiedBytecodeCompiler.cs`; fallback route: existing expression-program literal/spread evaluation; sync/resumable: both. Manifest row `A51h1` pins open source-presence anchors for spread-source span restrictions and computed object-key restrictions. Keep call/private/dynamic-key sources outside this child unless the current measured span helper already admits that shape.
- [ ] **A51h2** Array/object/template literal-container span helper gaps — owner: `TryAppendSimpleArrayLiteralSpan`, `TryAppendSimpleObjectLiteralSpan`, `TryAppendSimpleTemplateLiteralSpan`, and the matching production-eligibility measurers; fallback route: existing expression-program literal/spread evaluation; sync/resumable: both. Manifest row `A51h2` pins the helper source anchors plus the array/template boundary diagnostics that keep this literal-container lane independently trackable.
- [ ] **A51i** Computed/optional/private property-read and receiver-boundary shape gaps — owner: property-read lowering; fallback route: existing expression-program property evaluation; sync/resumable: both. Private receiver-prefix value-read chains (`receiver.#child.value`, `receiver.#child[key]`), private receiver-prefix named calls (`receiver.#child.value()`), private receiver-prefix public named/computed writes (`receiver.#child.value = value`, `receiver.#child[key] = value`), one-hop private receiver-prefix public named/computed updates (`receiver.#child.value++`, `receiver.#child[key]++`), and single-hop optional private reads (`receiver?.#field`) now route through sync production bytecode; private receiver-prefix compound/logical/private-terminal writes, deeper private-prefix update chains, chained optional-private neighbors, and unsupported property-read base diagnostics stay outside this slice.
- [ ] **A51j** Property write, compound/logical write, update, delete, name-inference, key-span, and RHS-span gaps — owner: property mutation lowering; fallback route: existing expression-program mutation evaluation; sync/resumable: both. Partial: one-hop private receiver-prefix public named/computed update terminals (`receiver.#child.value++`, `receiver.#child[key]++`) now route by composing the private receiver read with `UpdateNamedProperty` / `UpdateComputedProperty`; deeper private-prefix update chains and compound/logical/private-terminal mutation neighbors remain gated.
- [ ] **A51k** Operand-span compiler diagnostics — owner: expression-span lowering; fallback route: existing expression-program evaluation; sync/resumable: both. Manifest row `A51k` pins open source-presence anchors for simple-binary operator restrictions, unsupported simple-operand helper diagnostics, and call/private/dynamic-neighbor operand boundaries. Conditional consequent/alternate simple-literal-span diagnostics are tracked by A51h, and those call/private/dynamic-neighbor operands remain outside the simple value child.
- [x] **A51k1** Simple value operand-span helper lane — owner: `TryAppendSimpleBinaryOperandSpan`, `TryAppendSimpleUnaryOperandSpan`, `TryAppendSimpleNestedOperandSpan`, `TryAppendBoundedSimpleControlExpressionOperandSpan`, and mirrored `UnifiedBytecodeProductionEligibility` walkers including `TryMeasureSimpleLiteralValueOperandSpanCore`, `TryMeasureSimpleBinaryOperandSpan`, `TryMeasureSimpleUnaryOperandSpan`, `TryMeasureSimpleControlExpressionOperandSpan`, and `TryMeasureSimpleConditionalExpressionOperandSpan`; fallback route: existing expression-program evaluation for excluded neighbors; sync/resumable: both. ✅ (Codex): nested simple binary/unary/logical-control/conditional operand spans are inventoried separately when every nested operand is already an admitted literal-value span. This row is an inventory/proof rebaseline only; it does not claim call/private/dynamic-neighbor operands or the A51h conditional simple-literal-span diagnostics.
- [x] **A51m** Measured property-read span rollback diagnostics — owner: measured span helpers; fallback route: existing property-read expression evaluation for non-admitted shapes; sync/resumable: both. ✅ (Codex): source-guarded compiler diagnostics now pin named, optional named, optional named-chain, and computed measured property-read span rollback helpers. The measured computed span helper now has its own rollback diagnostic template and restores unified instructions, literal constants, and string constants before returning false after key-span emission failure. This is diagnostics/guardrail closure, not runtime widening.

**A51l retired/decomposed:** catch/try/driver cleanup diagnostics are no longer a standalone owner leaf. Catch-binding and dynamic environment storage diagnostics belong to A51c; invalid targets, active try-completion targets, and remaining loop-control topology belong to A51a2; iterator, for-in, `yield*`, resume-target, driver state-slot, and iterator-close state-slot diagnostics belong to A51d; logical property-write cleanup-start diagnostics belong to A51j; resumable finally-cleanup guards remain explicit eligibility declines until the VM owns those semantics.
- [x] **A52** `debugger;` statement — *(new leaf from P0.1)* — ☑/☑ — ✅ (commit d01faf31f): was not a keyword — lexed as Identifier and threw `ReferenceError` at runtime. Now a reserved word (`TokenType.Debugger`) lowered to the already-owned `EmptyStatement` no-op (admitted on every path; no new opcode/eligibility change). Routes through both sync-function and script fast paths. Reserved-word rejection (`var debugger`) + property-name usage (`o.debugger`) preserved.

## Phase B — Resumable-VM parity + suspension machinery (57 items)

Gated by `TryFindUnsupportedResumableOpcode@895` (opcode allowlist) and
`IsSupportedResumableInstruction@846` (instruction allowlist). Most are
sync-admitted ☑ but resumable-declined ☐ purely because they're absent from
these allowlists — mechanical extensions against existing sync VM handlers.

- [x] **B1** async / async-arrow ordinary body — extend await-body admission — sync n/a / res ☑ ✅ (commit 48ca3dc12): `var x = await p` now routes through the resumable VM — the awaited declaration lowers to `<awaited ops> → AwaitValue → InitializeSlot`, so the settled value lands in the flat slot after the suspension and a later `LoadSlot` reads it correctly. Multi-sequential-await sound. `__debug`-instrumented bodies decline resumable routing (introspection walks the env chain, not flat slots).
- [x] **B2** generator body — extend yield* / remaining yield shapes — n/a/☑ ✅ (Codex): the old synchronous generator `yield*` carve-out is closed. Local/param delegated iterables are pinned by `ResumableAlreadyRoutingPinTests`; free/dynamic delegated iterables and free call targets were closed by B38 and pinned by `UnifiedBytecodeProductionInvocationTests`. Remaining delegated async-generator awaited-source work belongs to B39's `yield* await ...` follow-up shape, not this sync-generator gate.
- [x] **B3** **async generator `async function*` — first EvaluateResumable route modeled** — n/a/◐ ✅ #3135 + Codex: simple-parameter direct-yield bodies can route through `UnifiedBytecodeVirtualMachine.ExecuteResumable`; non-awaited async-generator `yield*` is admitted by B39, and async-generator `yield* await ...` now emits `<awaited source> -> AwaitValue -> YieldStar` before the same VM-owned delegation driver.
- [x] **B4** Property write `o.x=v` in resumable — ☑/☑ ✅ #3114
- [x] **B5** Computed property write `o[k]=v` — ☑/☑ ✅ #3114
- [x] **B6** Property update `o.x++` / `o.x+=v` — ☑/☑ ✅ #3117
- [x] **B7** Computed property update `o[k]++` — ☑/☑ ✅ #3117
- [x] **B8** Slot update `x++` / `x+=v` (UpdateSlot) — ☑/◐ ✅ #3115 (var/param admitted first; lexical `let`/`const` slot updates restored by B8a). Also fixed the latent const **plain-assignment** gap `const x=1; x=2` in #3116.
- [x] **B8a** *(follow-up, option a)* Thread a static const-slot bitmap from scope analysis → `ExecutionPlan`/`ActivationSlotShape` → `UnifiedBytecodeResumeState`, so the resumable VM can raise `TypeError: Assignment to constant variable` itself and restore `let`-write/`let`-update fast-path. ✅ (Codex): `UnifiedBytecodeResumeState` now carries the const-slot bitmap seeded from `UnifiedBytecodeProgram.ConstSlotIndices` and extended by `TdzHeadInit`; resumable `StoreSlot`/`UpdateSlot` check it before mutation/coercion. The old lexical-slot assignment/update declines and expression-program slot-reference guard were removed, so `let` assignment/update routes while `const` assignment/update routes and throws from the VM.
- [x] **B9** Property delete `delete o.x` — ☑/☑ ✅ #3117
- [x] **B10** Computed property delete `delete o[k]` — ☑/☑ ✅ #3117
- [x] **B11** `new C(args)` construct — ☑/☐ ✅ #3152
- [x] **B12** super call/construct — ☑/☑ ✅ (Codex): resumable generator/async method bodies now admit named/computed super-member calls after suspension via `EnsureSuperReference`, `PrepareNamedSuperCallTarget`, and `PrepareComputedSuperCallTarget`, using the captured dynamic method environment on `UnifiedBytecodeResumeState.CallingEnvironment` so `super.m()` / `super[k]()` preserve the derived receiver. Super-construct remains intentionally declined: derived constructors themselves cannot be generator/async bodies, and lexical constructor-state inheritance stays on existing activation gates until a legal resumable source shape is proven.
- [x] **B13** super property read `super.x` — ☑/☑ ✅ (Codex): stale row closed by current source proof. `ExecuteResumable` handles `EnsureSuperReference`, `GetNamedSuperProperty`, and `GetComputedSuperProperty` against `UnifiedBytecodeResumeState.ResumableSuperBinding`; generator, async, and async-generator method bodies route and read base properties after suspension. Proof: `UnifiedBytecodeResumableSuperPropertyReadTests`.
- [x] **B14** super property write/update — ☑/☑ ✅ (Codex): stale row closed by current source proof. `ExecuteResumable` handles `SetNamedSuperProperty`, `SetComputedSuperProperty`, `UpdateNamedSuperProperty`, and `UpdateComputedSuperProperty` with the resumable super binding and body strictness. Proof: `UnifiedBytecodeResumablePropertyWriteTests` and `UnifiedBytecodeResumablePropertyUpdateDeleteTests`.
- [x] **B15** Optional member/computed call `o?.m()` / `o?.[k]()` — ☑/☑ ✅ #3159
- [x] **B16** Object literal `{a,b:v,...spread}` — ☑/☑ ✅ #3151
- [x] **B17** Array literal `[a,,b,...spread]` — ☑/☑ ✅ #3151
- [x] **B18** `#field in obj` — ☑/☑ ✅ #3153
- [x] **B19** `new.target` (LoadNewTarget) — ☑/☑ ✅ (Codex; leak fixed #3150 — per-activation new.target threaded onto the resume state: undefined for ordinary generators/async, inherited for async arrows)
- [x] **B20** `import.meta` — ☑/☑ ✅ (commit a8e64adb6): `import.meta` routes through ExecuteResumable — `LoadImportMeta` handler resolves `Symbol.ImportMeta` against the captured module env (stable across suspension); unbound → ReferenceError. Allowlist 1:1.
- [x] **B21** Tagged-template / template object — ☑/☑ ✅ (Codex): ordinary identifier/member tagged-template calls now route through production unified bytecode on sync and resumable paths. Bare identifier tags lower through `LoadIdentifierCallTarget` so the VM call boundary sees the same `<this, callee, args...>` stack contract as ordinary calls; `LoadTemplateObject` is admitted by the complex call-argument stack walker, emitted by the generic expression compiler, allowlisted for `ExecuteResumable`, and handled with the same descriptor/callsite identity cache as the sync VM. Proof covers identifier tags, member tags with receiver preservation, generator tags, substitution across `yield`, and async tags after `await`.
- [x] **B22** Regex literal — ☑/☑ ✅ #3118
- [x] **B23** Nested function literal — ☑/☑ ✅ (Codex): non-capturing nested function literals inside generator/async bodies route through `ExecuteResumable` via `LoadFunctionLiteral` and `EnsureHasName` (including object method/accessor literals from A35b-e). Generator, async-function, and async-generator nested literals that capture root body locals route through a materialized resumable body environment that mirrors activation flat slots into closure-visible bindings and keeps slot writes synchronized across yields/awaits, including direct in-body calls after suspension. Nested arrows that need lexical `this` and private-name scopes route after the resumable invoker proves the context and creates a per-call invocation environment for the literal to capture. Function declarations nested inside literals now route too: the outer resumable VM creates the literal, and the literal's normal invocation path owns its declaration instantiation while capturing the materialized resumable body environment when inner declarations close over outer slots.
- [x] **B24a** Class expression constructor/default-constructor creation — sync ◐ / res ☑ ✅ (Codex): constructor-only class expressions now route through `ExecuteResumable` via `LoadClassLiteral`. The resumable handler creates the class value with the captured calling environment and preserves explicit constructors, implicit base constructors, and implicit derived constructors whose `extends` value is environment-resolved; static blocks/elements, private members, accessors, computed members, member-super shapes, non-B24b field shapes, and `extends` expressions that read resumable activation slots still decline for B24c-B24i / later class-definition environment work.
- [x] **B24b** Class expression instance fields — sync ◐ / res ◐ ✅ (Codex): resumable generator/async bodies now admit `LoadClassLiteral` for the public non-computed instance-field class-expression subset whose field initializers do not capture activation slots, create the class value from the captured calling environment inside `ExecuteResumable`, and also admit the mixed public non-computed static+instance field subset when instance initializers stay activation-safe. Computed, public accessor/member, extends/super, static-block, activation-capturing field-initializer, and private shapes outside B24e/B24f remain declined for later class-literal slices.
- [x] **B24c** Class expression static fields — sync ◐ / res ◐ — simple public non-computed static-field class expressions route through `ExecuteResumable` via guarded `LoadClassLiteral`, including pure public static field class literals whose top-level function-literal initializers create activation-capturing closures, mixed public static+instance field class literals whose instance initializers do not capture activation slots, and public non-computed static-field class literals with `extends` whose initializers compile as standalone unified bytecode and do not create closures; broader class-definition evaluation remains delegated and B24h neighbors still decline.
- [x] **B24d** Class expression static blocks — sync ☑ / res ☑ ✅ (Codex): static-block-only class expressions now route through `ExecuteResumable` via guarded `LoadClassLiteral`, using the existing class-literal slot environment so activation reads/writes stay synchronized after class creation. Eligible static-block bodies now execute their cached plan through production unified bytecode before falling back to `ExecutionPlanRunner.RunScript`; static blocks that create activation-capturing function literal closures now request the materialized body environment so those closures observe later activation-slot mutations. Static blocks with nested declarations or non-production-eligible plans remain declined until the broader materialized class-definition environment route owns that lifetime.
- [x] **B24e** Class expression private fields / branding — sync ◐ / res ☑ ✅ (Codex): resumable routing now conditionally admits `LoadClassLiteral` for class literals with private instance fields plus constructor/default-constructor code, no extends that read resumable activation slots, and no static blocks/elements, static/computed fields, or private members. The route still uses class-definition creation for brand setup, but the admitted shape is now pinned by `UnifiedBytecodeResumableClassLiteralPrivateMemberTests`.
- [x] **B24f** Class expression private methods / private accessors — sync ◐ / res ☑ ✅ (Codex): resumable routing now conditionally admits `LoadClassLiteral` for class literals with private instance methods/accessors and no extends, fields, static blocks/elements, static members, computed members, public members, or activation-slot captures in constructor/member bodies; neighboring non-B24f class-literal shapes remain owned by their B24 rows.
- [x] **B24g** Class expression public accessors — sync ◐ / res ☑ ✅ (Codex): resumable generator/async bodies now admit `LoadClassLiteral` for the public non-computed instance getter/setter class-expression subset whose accessor bodies do not capture activation slots, including accessor bodies that use `super`; descriptor and receiver behavior still reuse class-definition member assignment, while static/computed/private/capturing accessor neighbors and mixed field/accessor shapes remain declined for B24h/B24i or materialized-body-environment follow-ups.
- [ ] **B24h** Class expression computed member names / computed field names — sync ◐ / res ◐ — public static/instance computed fields/methods/accessors now route through resumable `LoadClassLiteral` when computed name programs either avoid resumable activation slots or use owned activation read/write/update/reference-store operations, including activation-target calls whose argument region is already admitted by the production complex-call-argument walker, including spread-call argument lists, and immediate zero-argument computed-name IIFEs like `[(() => key)()]`, including IIFEs that create escaping activation-capturing closures under the materialized body-environment route. Activation binding deletes and activation-dependent constructs in computed-name programs remain no-route residue, including `[delete key]`, `[new MakeName()]`, `[new MakeName(key)]`, and `[new MakeName(...args)]`, with manifest rows pinned to `UnsupportedPlanShape` and the B24h decline reasons. Public computed instance field initializers may now read resumable activation slots or create activation-capturing function literals under the same materialized body-environment route; public computed static field initializers may now directly read/write/update resumable activation slots or create activation-capturing closures through the materialized body-environment route; public computed member bodies may now capture resumable activation slots through the same materialized body-environment route, while constructor-body activation captures stay declined until the broader class-definition environment bridge owns construction-time closure state; public computed members and field initializers that use `super` now route through the same `LoadClassLiteral` path when the superclass expression does not read resumable activation slots; neighboring public non-computed fields/methods/accessors, noncapturing or activation-capturing private instance methods/accessors, non-static/non-computed private instance fields, activation-safe private static fields, including direct activation-reading/updating and `super` private static initializers, and eligible static blocks whose bodies route through production unified bytecode stay inside the same subset, including static blocks that create activation-capturing function literal closures, descriptor-backed function declarations, nested class declarations, and declaration-free single-literal direct eval statements through the materialized body-environment route. Remaining B24h class-expression work: computed-name activation delete/construct residue, constructor-body activation captures, and the broader class-definition environment bridge. Runtime-source direct eval, otherwise non-production-eligible static-block plans, and static-block IR fallback stay named as separate residue anchors, not B36 class-declaration work and not terminal retirement claims. Current executable/open proof: `B24h-computed-name-activation-delete-declines`, `B24h-computed-field-name-activation-delete-declines`, `B24h-computed-name-activation-construct-declines`, `B24h-computed-name-activation-construct-argument-declines`, `B24h-computed-name-activation-construct-spread-declines`, `B24h-computed-member-constructor-captures-activation-declines`, `B24h-computed-static-block-runtime-source-direct-eval-declines`, `B24h-computed-static-block-non-production-static-block-stays-open`, and `B24h-computed-static-block-ir-fallback-stays-open`.
- [x] **B24i** Class expression `super` in members/field initializers — sync ◐ / res ☑ ✅ (Codex): activation-safe public non-computed instance methods, instance field initializers, accessor bodies, public static methods/accessors, public non-computed static field initializers that use `super`, and mixed public non-computed static fields plus static methods/accessors now route through resumable `LoadClassLiteral` when field initializers compile as standalone unified bytecode and member/constructor bodies do not capture activation slots; computed/private/capturing or static-block-neighbor super-bearing class-definition shapes still decline before creation until bytecode owns more of class-definition evaluation.
- [x] **B25** `typeof unresolvedFreeVar` — ☑/☑ ✅ #3163
- [x] **B26** Dynamic free write `freeVar=v` — ☑/☑ ✅ (Codex): free/global plain writes now route through `ExecuteResumable`. The pending `AssignmentReference` from `ResolveDynamicIdentifierReference` is stored on `UnifiedBytecodeResumeState`, so `freeVar = yield` resumes with the exact target resolved before the suspension; `StoreDynamicIdentifierReference` keeps sync name-inference and strict/unresolvable-reference semantics.
- [x] **B27** Dynamic free update `freeVar++` — ☑/☑ ✅ (commit 0c2707532): `UpdateDynamicIdentifier` admitted to the resumable route (commit 71be17015), free/global update routes; pinned.
- [x] **B28** `delete freeVar` — ☑/☑ ✅ (commit 0c2707532): `DeleteDynamicIdentifier` is self-contained (name+env+isStrict → bool, no pending reference), new resumable handler + allowlist entry (1:1). Routes; same-global-across-suspension verified.
- [x] **B29** Dynamic reference plumbing (compound free-var ops) — ☑/☑ ✅ (Codex): free/global and captured compound/logical writes now route through `ExecuteResumable`. The compiler lowers non-static targets through `ResolveDynamicIdentifierReference` -> `LoadDynamicIdentifierReference`, then RHS/binary or short-circuit branching, and finally `StoreDynamicIdentifierReference`; the logical short-circuit branch emits `PopDynamicIdentifierReference` so the pending reference stack is balanced without writing. Finally-body dynamic mutations still stay behind the existing early-close cleanup guard and belong to B32.
- [x] **B30** `for-of` sync driver across suspension — ☑/☑ ✅ (Codex; #3123 hardened the guard: suspending/nested try-finally correctly declines, restoring 13 generator try/finally tests)
- [x] **B31** `for-in` driver across suspension — ☑/☑ ✅ (Codex)
- [x] **B32** try/catch/finally across suspension — ☑/☑ ✅ (Codex): try/**finally** with yield-in-try was already pinned; try/**catch** now routes through `ExecuteResumable` for optional/simple catch bindings. `UnifiedBytecodeResumeState` carries a resumable try-frame stack with descriptor index, thrown value, catch-used, finally scheduling, and pending completion so explicit throws and `.throw(...)` resumes enter catch, preserve catch bindings across suspension, and run following cleanup. Destructuring catch bindings still stay declined.
- [x] **B33** `break`/`continue` across suspension (driver cleanup) — ☑/☑ ✅ (Codex): resumable generator/async bodies now admit `BreakInstruction` and `ContinueInstruction`, compiling them to the existing VM-owned `Break`/`Continue` control-flow opcodes. Driver cleanup stays on `TryCleanupDriverStatesForControlTargetResumable`; sync generator close cleanup now resolves plain `Iterator.return()` results synchronously while async functions/generators keep async-step scheduling. Labeled generator break/continue route-hit pins cover iterator close/target behavior.
- [x] **B34** Array destructuring across suspension — ☑/☑ ✅ (Codex)
- [x] **B35** Object destructuring across suspension — ☑/☑ ✅ (Codex)
- [ ] **B36** Nested function/class declaration hoisting in resumable body — ☑/◐ — ◐ partial: direct root, non-capturing function-scoped declarations now route in generators, async functions, and async generators. The sync-generator, async-function, and async-generator routes also admit direct root helpers that capture root body locals after invocation setup materializes the resumable body environment and creates the helper against that environment, so the helper observes flat-slot mutations across `yield`/resume or `await`/resume, including direct in-body helper calls after suspension. Recursive and sibling helper graphs now route through the same pre-populated declaration environment. The resumable invokers pre-populate every admitted root helper's VM flat slot before `ExecuteResumable`; slot resolution is performed against the compiled `UnifiedBytecodeProgram` so async synthetic resume slots cannot shift the helper into the wrong slot. A43 now admits descriptor-backed block/Annex B declarations via materialized block environments. Direct root simple class declarations now route through `DeclareClass` after the resumable invoker materializes the body environment; plain class declarations with standalone-unified-compiled superclass expressions now route through the same opcode, including activation-dependent superclass expressions and activation-safe explicit public derived constructors, and preserve superclass construction/`instanceof` while requiring the constructor's own production bytecode route. Eligible static-block-only class declarations now route too, including activation-capturing closures assigned from static blocks: the outer `DeclareClass` path synchronizes the class-declaration environment, the resumable invoker materializes the body environment when static-block plans create activation-capturing closures or descriptor-backed function declarations, and each static-block body must itself hit production unified bytecode instead of `ExecutionPlanRunner.RunScript`. Direct root public non-computed static-field class declarations without `extends` now route when field initializers compile as standalone unified bytecode, including activation-capturing closure-producing static field initializers that observe later resumable slot mutations and mixes with public non-computed static methods/accessors whose bodies do not capture activation. Direct root class declarations that mix public non-computed static fields with eligible static blocks now route as well, with field initializers compiling as standalone unified bytecode, static blocks preserving class-definition order, descriptor-backed static-block function declarations observing later resumable slot mutations, and nested class declarations executing through the static-block VM even when their methods capture the materialized body environment. Public instance computed-member `extends` declarations now route by composing the standalone superclass compile check, the B36 constructor predicate, and the B24h computed public member gate, including direct activation-call computed names like `[key = read()]()` and immediate computed-name IIFEs that create escaping activation-capturing closures under the B24h materialized body-environment subset. Public non-computed instance method/accessor `extends` declarations now route when the member set is ordinary public instance methods, getters, or setters without `super`; this preserves superclass construction/`instanceof` and accessor descriptor behavior but does not claim broad member-body production routing. Public instance super-member and public static super-member `extends` declarations now route even when their member bodies capture activation, public instance super-field declarations route even when field initializers capture activation, and public non-computed static-field `extends` declarations now route when each initializer compiles as standalone unified bytecode, including closure-producing static fields and static `super` field initializers. Declaration-free single-literal direct eval statements inside static blocks now route through the same static-block production path. Remaining B36 class-declaration/resumable-helper work is now split into exact manifest rows, separate from B24h class-expression residue: `B36-resumable-dynamic-eval-helper-activation-decline-stays-open`, `B36-resumable-arguments-eval-helper-decline-stays-open`, `B36-resumable-helper-direct-eval-cache-decline-anchor-stays-open`, `B36-resumable-helper-synthetic-activation-capture-decline-anchor-stays-open`, `B36-class-declaration-static-block-runtime-source-direct-eval-declines`, `B36-class-declaration-non-production-static-block-stays-open`, `B36-class-declaration-static-block-ir-fallback-stays-open`, `B36-deferred-class-definition-environment-bridge-stays-open`, `B36-class-declaration-static-field-shape-guard-stays-open`, `B36-class-declaration-static-member-shape-guard-stays-open`, `B36-class-declaration-computed-member-activation-capture-stays-open`, and `B36-class-declaration-computed-field-activation-capture-stays-open`. Correct for the remaining subset via IR.
- [x] **B37** Scaffolding opcodes (Tdz/EnsureHasName/ToString/ThrowReferenceError) in resumable — ☑/◐ ✅ (commit a8e64adb6 + Codex): `ToString` (untagged template per-hole `String()` coercion) admitted to resumable (handler + allowlist 1:1); `Tdz`/`TdzHeadInit` already surfaced by existing `LoadSlot`; `EnsureHasName` is now admitted for non-capturing function-literal name inference. `ThrowReferenceError` is admitted at the synthetic opcode/handler level and routes through resumable throw/catch/finally handling; its source producer (`delete super[...]`) still declines before VM entry.
- [x] **B38** `yield* freeIter` over free/dynamic iterable — n/a/☑ ✅ (Codex): the targeted free/dynamic iterable guard is deleted. Sync generator `yield*` now routes through `ExecuteResumable` when the iterable is a free binding or free call target (`yield* values`, `yield* makeIterator()`), with the VM preserving the original delegated iterator result object, sending `undefined` to the first delegated `next()` even when the outer first `next(value)` carries a value, and synchronously unwrapping promise-like delegated `throw`/`return` results to match the IR runner.
- [x] **B39** async `yield* asyncIterable` — n/a/☑ ✅ (Codex): async-generator `yield*` over delegated async iterables now routes through `ExecuteResumable` for both direct delegated sources and `yield* await ...` awaited sources. The awaited-source form emits the source expression, `AwaitValue`, then `YieldStar`, so the settled iterable flows into the existing async-generator delegation driver. The VM awaits delegated `next`/`return`/`throw` iterator results through the existing async-generator `PendingAwait` settlement bridge and preserves first `next(undefined)`.
- [x] **B40** `with(obj){}` in generator/async body — n/a/☑ ✅ (Codex): plain non-awaited `with` now routes through `ExecuteResumable`. `EnterWithInstruction` / `LeaveWithInstruction` compile to `EnterWith` / `LeaveWith`, and `UnifiedBytecodeResumeState.CurrentEnvironment` persists the active dynamic environment across `yield` / `await` so dynamic reads, writes, calls, and closure/class creation resolve through the with scope after suspension. Dynamic-scope function plans keep materialized activation-slot metadata even when normal slot rewriting is skipped, so `with` object expressions can resolve parameters/locals before `EnterWith`. Manifest proof `D3-resumable-active-with-current-environment-routes` pins this admitted active-current-environment lane. Awaited with-object evaluation remains B43/D3 residue.
- [x] **B41** `for await (x of asyncIter)` async-iterator driver — ☑/☑ ✅ (Codex): stale row closed by current source proof. `EvaluateResumable` admits lowered `for await...of` bodies with `IteratorDriverKind.Await`; `ExecuteResumable` awaits async iterator `next()` results and yielded values, awaits async `return()` during close, and routes custom async iterables and sync iterables yielding promises through the resumable fast path. Proof: `UnifiedBytecodeResumableForOfTests.AsyncForAwaitOfCustomAsyncIterator_RoutesResumableAndAwaitsNextResults`, `AsyncForAwaitOfReturn_AwaitsAsyncIteratorReturnBeforeSettling`, `AsyncForAwaitOfSyncIterableYieldingPromises_RoutesResumableAndAwaitsValues`, and `EvaluateResumable_ForAwaitOfDriver_AdmitsAsyncIteratorDriver`.
- [x] **B42** `for(k in await p)` awaited for-in source — n/a/☑ ✅ (Codex)
- [x] **B43** Awaited with-object `with(await x){}` — ☑/☑ ✅ (Codex): classified under the D3 terminal dynamic-residue quarantine. The lowering still carries `EnterWithInstruction.AwaitedProgram`; production eligibility now declines it with explicit D3 wording instead of leaving it as an ambiguous async/resumable routing gap. Manifest proof `D3-awaited-with-object-residue-stays-declined` keeps that wording source-pinned.
- [x] **B44** Awaited binding/destructuring decl `let [a]=await x` — ☑/☑ ✅ (commit 48ca3dc12): awaited destructuring lowers to `<awaited ops> → AwaitValue → ApplyDeclarationBindingTarget` (new resumable VM handler that runs the synchronous destructuring against the resume state's CallingEnvironment after the suspension and writes each binding to its flat slot). Allowlist 1:1.
- [x] **B45** Resumable instruction-allowlist default (master plan-level gap) *(→ P0.3)* — n/a/n/a ✅ decomposed by P0.3: the current non-allowlisted instruction-record inventory is empty and drift-checked under `Resumable Instruction Allowlist Gaps`.
- [x] **B46** Resumable opcode-allowlist default (master opcode-level gap) *(→ P0.3)* — n/a/n/a ✅ decomposed by P0.3: the 5 current non-allowlisted opcodes are now named and drift-checked under `Resumable Opcode Allowlist Gaps`; the admitted allowlist remains 1:1 with `ExecuteResumable`.
- [x] **B47** Resumable compiler `TryCompile` wrap *(decomposed by P0.2)* — n/a/see A51a1/A51a2, A51b-A51k, A51m, and B47a.
- [x] **B47a** Resumable-only compiler declines for `yield*` state slots and synthetic resume targets — n/a/☑ ✅ (Codex): current compiler slot layout adds generator-lowered synthetic resume/result and `yield*` driver-state symbols before flat-slot mapping, so sync generator `yield*` compiles and routes through `ExecuteResumable` instead of declining on missing activation-slot metadata. Existing focused coverage pins `YieldStar`, `StoreResumeValue`, public generator route hits, delegated return/throw, non-awaited async-generator `yield*` route hits, and async-generator `yield* await` awaited-source route hits.

## Phase C — Top-level / script route (3 items)

- [x] **C1** Script `typeof <ident>` reading block-scoped lexical (stale flat-slot liveness): `for(let i){}; typeof i` — *UnsupportedPlanShape* — ☑/n/a ✅ (Codex)
- [x] **C2** Script with no `ScriptCompletionSlot` — ☑/n/a ✅ (Codex)
- [x] **C3** Script inheriting any per-shape decline (union gate; closes via A/B) — *UnsupportedPlanShape* — ☑/n/a ✅ (Codex): focused C3 proof rows now pin top-level scripts that compose admitted property reads/calls, control flow, dynamic global reads/calls, completion-slot endings, and script-scope `var` / `let` / `const` destructuring through the shared production unified-bytecode gate. Eval-injected script bindings remain dynamic residue, so future non-residue script declines must be added deliberately instead of silently falling back.

## Phase D — Dynamic quarantine gates (5 items)

- [x] **D1** Direct-eval quarantine gate (multi-arg/spread + Call-op IsDirectEval → interpreter) — ☑/n/a ✅ (Codex): closed terminal dynamic-residue guardrail only. Focused eligibility coverage now pins multi-argument direct eval, spread direct eval, and a synthetic non-admitted `Call` op with `IsDirectEval` as pre-VM `CallDependency` declines, while runtime route/no-route coverage proves those residue shapes evaluate correctly without `unified-bytecode-production-fast-path` and the admitted one-argument non-spread non-injecting direct eval route remains green.
- [x] **D2** eval-injected runtime binding quarantine — ✅ (Codex): closed terminal dynamic-residue guardrail only. Direct-eval literal sources containing `var` / `let` / `const` / `function` / `class` declarations decline with `CallDependency`, identifier-loaded eval source stays on the IR/eval path and computes correctly, and declaration-free `eval("local")` / non-injecting direct eval still route through the production fast path. Proof anchors: `Evaluate_DirectEvalDeclarationLiteral_DeclinesEvalInjectedRuntimeBinding`, `Evaluate_DirectEvalIdentifierSourceWithDynamicRead_DeclinesEvalInjectedRuntimeBinding`, `DirectEvalDeclarationDynamicLookup_StaysOnIrPath`, `DirectEvalIdentifierSourceWithRuntimeBinding_StaysOnIrPath`, `DirectEvalIdentifierCall_UsesCallerEnvironmentOnProductionFastPath`, and `DirectEvalThenOrdinaryDynamicReadStoreCall_UsesUnifiedBytecodeProductionFastPath`.
- [x] **D3** `with` quarantine boundary ✅ (Codex): closed terminal dynamic-residue guardrail only. Non-awaited sync and resumable `with (obj)` bodies are admitted through the VM-owned current-environment lane, pinned by manifest proof `D3-resumable-active-with-current-environment-routes`. The remaining D3 residue is awaited with-object evaluation and live `with` scopes outside that persisted-current-environment model; `EvaluateResumable` declines awaited `EnterWithInstruction.AwaitedProgram` with explicit D3 wording, pinned by `D3-awaited-with-object-residue-stays-declined`. Retained live-`with` closure chains stay separately represented under open A2 proofs `A2-retained-with-closure-read-stays-open` and `A2-retained-with-closure-receiver-call-stays-open`.
- [x] **D4** `Function(...)` produced-body quarantine (body recurses into gate) ✅ (Codex): dynamic `Function` / `new Function` produced bodies now carry an explicit origin marker and are declined before the sync production unified-bytecode route, while the ordinary construction/call boundary and adjacent non-dynamic functions remain eligible. Pinned by `DynamicFunctionConstructorQuarantineTests.NewFunctionProducedBody_StaysOffProductionUnifiedBytecodeButOrdinaryFunctionStillRoutes`.
- [x] **D5** Standing CI gate: assert no `isDynamicResidue=false` decline ever fires on the corpus ✅ (Codex): `BytecodeNonResidueDeclineRatchetTests` pins the non-residue corpus and known-open count. The ratchet now admits optional computed delete, the stale non-awaited resumable `with` dynamic-residue row, top-level lexical script destructuring, and the B23/B36 nested declaration proof rows. The known-open non-residue count is now `0`.

## Phase E — Retire the fallback tiers (10 items)

- [x] **E1** *(moved to P0.5 — dead `LabelControlFlow` deletion)*
- [x] **E2** Promote each wrapped `TryCompile` reason to a named decline *(= P0.2; A51/B47 decomposed and drift-guarded)*
- [x] **E3** Diff opcode enum vs admit-switch; name every gap *(= P0.3)* ✅
- [ ] **E4** Retire the remaining `ExpressionProgram` (tier-1) bridge inventory — source-retirement proof now lives in `docs/plans/bytecode-proof-manifest.json`: the ordinary sync generic simple-return-expression branch in `TryInvokeIrFast` no longer executes `ExpressionProgram`, the profiler-only `ExpressionProgram` loop now compiles through standalone unified bytecode instead of `ExecutionPlanRunner`, and `ExecutionPlanRunner.EvaluateStandaloneExpressionProgram`, `TypedAstEvaluator.EvaluateLoweredExpressionProgram`, `TypedAstEvaluator.EvaluateDynamicExpressionProgram`, `ExecutionPlanRunner.ApplyStandaloneBindingTargetProgram`, `ProfileEvaluateLoweredExpressionProgramLoop`, and `ProfileEvaluateExpressionProgramLoop` are deleted and source-ratcheted. Class `extends`, class-field initializer, class-property-name, dynamic-expression payloads, and external lowered binding-target payloads now execute standalone `ExpressionProgram` payloads through `UnifiedBytecodeExpressionProgramExecutor` and the unified VM. The E4 row remains open only for the live `UnifiedBytecodeExpressionProgramExecutor.ExecuteDynamic` dynamic payload bridge; runner-internal `ExpressionProgram` evaluation is E5b-E5d runner-retirement inventory, not an E4 closure blocker.
- [x] **E5a** Eligible static-block bodies route before runner fallback — admitted static-block routing is now its own completed child-owned row, not ordinary runner-retirement residue. Manifest proof `E5-static-block-body-routes-when-eligible` keeps this lane separate from the still-open `ExecutionPlanRunner.RunScript` fallback in `ClassDefinitionExtensions.ExecuteStaticBlock`.
- [ ] **E5b** ExecutionPlanRunner entrypoints remain finite classified anchors — ordinary runner retirement still requires the remaining runner construction and entry call sites to disappear, but broad `ExecutionPlanRunner` type or Core entry-method presence is no longer counted as a generic hot-path blocker. Manifest proofs `E5-ir-runner-type-still-present`, `E5-ir-runner-script-entry-still-present`, `E5-ir-runner-sync-entry-still-present`, and `E5-ir-runner-async-step-entry-still-present` are allowlist gates that tie construction, `RunScript`, `RunSync`, and `ExecuteAsyncStep` usage to E5c/E5d classified fallback owners until those owners are tombstoned.
- [ ] **E5c** Script fallback remains ordinary runner-retirement while static-block fallback is classified declined residue — script fallback remains classified in `RunScriptViaClassifiedIrFallback`, which delegates to `ExecutionPlanRunner.RunScript` after `EvaluateScript` declines. Eligible class/static-block bodies attempt production unified bytecode first; declined static-block plans still fall back to `ExecutionPlanRunner.RunScript`, but that anchor is classified as explicit declined static-block residue rather than ordinary E5c script fallback retirement. Manifest proofs: `E5-script-ir-fallback-still-runs-execution-plan-runner` and `E5-static-block-ir-fallback-still-runs-execution-plan-runner`.
- [ ] **E5d** Function, class-constructor, and resumable declined-body runner fallbacks remain finite retirement anchors — ordinary sync `TryInvokeIrFast` no longer constructs `ExecutionPlanRunner` or calls `RunSync`, but the outer `InvokeWithContextSlow` ordinary runner remains classified through `CreateClassifiedOrdinarySyncFunctionFallbackRunner` after production unified bytecode, simple expression routes, and `SyncIrCallTrampoline` decline; class constructor residue remains separately classified through `CreateClassifiedClassConstructorFallbackRunner` so base/derived `this`, `new.target`, private brand, and instance-field initialization stay visible as a distinct open boundary. Async functions and sync generators still construct runner-backed fallbacks when `EvaluateResumable` declines. Manifest proofs: `E5-function-runner-fallback-still-constructs-runner`, `E5-class-constructor-runner-fallback-still-constructs-runner`, `E5-sync-generator-declined-body-runner-still-present`, and `E5-async-function-ir-step-runner-still-present`.
- [ ] **E5e** Terminal dynamic residue remains excluded from ordinary runner retirement — direct eval with runtime-source, multi-arg, spread, or declaration-injecting sources, awaited-with object evaluation, retained live `with` scopes outside the VM current-environment lane, eval-injected runtime bindings, and `Function(...)`-produced bodies remain A2/D1/D2/D3/D4-style quarantine boundaries outside ordinary E5 runner retirement.
- [x] **E6** Delete remaining `AsyncGeneratorInvoker` IR fallback after the async-generator route widens past the current #3135 direct-yield boundary. Direct yield, non-awaited `yield*`, awaited-source `yield* await`, delegated return/throw, pending-await settlement, try/finally cleanup, captured hoisted helpers, and declaration-free direct eval stay covered by the async-generator route proof pack. Declined async-generator bodies no longer construct the old `CreateClassifiedAsyncGeneratorDeclinedBodyRunner` helper or any renamed `_fallbackRunner` / `ExecuteFallbackRunnerStep` / `ExecuteAsyncStep` runner bridge; non-simple parameter lists now fail explicitly until resumable invocation owns IteratorBindingInitialization.

---

## Counts

| Phase | Items | Notes |
|---|---:|---|
| 0 — Make list finite | 5 | 5 complete / 0 open; Phase 0 inventory closure is complete |
| A — Sync admission | 81 | 64 complete / 17 open; row-accounted decline code / promoted compiler leaf inventory |
| B — Resumable parity + suspension | 57 | 55 complete / 2 open; class-expression decomposition added B24a-B24i |
| C — Script route | 3 | 3 complete / 0 open; closes mostly via A/B |
| D — Dynamic quarantine | 5 | 5 complete / 0 open; D1-D5 now pin terminal dynamic-residue boundaries |
| E — Retire tiers | 10 | 5 complete / 5 open; E1 is counted through P0.5, E2/E3 = P0.2/P0.3, E5a is admitted static-block routing, and E4/E5b-E5e remain active retirement/exclusion gaps |
| **Total** | **161** | row-accounted finite current burn-down list after Phase 0 decomposition; future source drift must update audited inventories |

**Status (146 row-accounted A+B+C+D checklist items):** 127 complete / 19 open. **The remaining non-retirement work is now concentrated in activation/dynamic residue boundaries (A1/A2), remaining A51 compiler leaves, class-expression non-production static-block/class-definition work (B24h), and B36 nested declaration/class contexts.** The remaining retirement work is E4 (remove `ExpressionProgram` from the hot path) plus E5b-E5d (remove `ExecutionPlanRunner` entrypoints and fallbacks after A/B/C parity); E5e preserves the terminal dynamic-residue exclusion boundary, and E1 is an alias for P0.5 rather than a second Phase E retirement closure.

Latest proof evidence (2026-06-06, E5 eligible static-block body VM route):
`TryInvokeIrFast` no longer executes a generic `SimpleReturnProgram` through
`EvaluateLoweredExpressionProgram`; that branch now declines to the classified
fallback after production bytecode, parameter/binary fast paths, and
`SyncIrCallTrampoline` decline. The profiler-only path has also moved off
`ExecutionPlanRunner`: `ProfileRunner` now compiles synthetic `ExpressionProgram`
profile cases to standalone unified bytecode and executes
`UnifiedBytecodeVirtualMachine` directly, while
`SourceGate_E4_ProfileExpressionProgramLoop_IsCompletelyRemoved` tombstones the
deleted profile bridge. `ExecutionPlanRunner.EvaluateStandaloneExpressionProgram`,
`TypedAstEvaluator.EvaluateLoweredExpressionProgram`, and
`TypedAstEvaluator.EvaluateDynamicExpressionProgram` are also deleted; class
`extends`, class-field initializer, class-property-name, dynamic-expression
payloads, and external lowered binding-target payloads now execute through
`UnifiedBytecodeExpressionProgramExecutor`, which compiles standalone expression
payloads to standalone unified bytecode and executes
`UnifiedBytecodeVirtualMachine`. `ExecutionPlanRunner.ApplyStandaloneBindingTargetProgram`
is deleted too: `TypedAstEvaluator.BindingTargetPrograms` now calls the static
lowered binding-target core directly instead of constructing a runner solely to
apply a lowered `BindingTargetProgram`. Eligible class static-block bodies now
attempt production unified bytecode before the `ExecutionPlanRunner.RunScript`
fallback, and `E5-static-block-body-routes-when-eligible` proves that lane in the
manifest. Focused verification passed:
`dotnet build tools/ProfileRunner/ProfileRunner.csproj` with 0 warnings,
`dotnet build src/Asynkron.JsEngine/Asynkron.JsEngine.csproj` with 0 warnings,
`./tools/profile bytecode` with the CPU call-tree root at
`UnifiedBytecodeVirtualMachine.Execute`, 157 class/activation tests, 71
`ExecutionPlanDiagnosticsTests` / `BytecodeProofManifestTests` /
`ExpressionProgramCoverageMapTests` tests, 221 binding/destructuring tests, 8
static-block/class tests, and 116 `AstFreeExecutionAssertionTests` runtime tests
after contract drift updates. The
production-source scan for `EvaluateLoweredExpressionProgram(` now returns no
matches, and
`SourceGate_E4_LoweredExpressionProgramBridge_IsCompletelyRemoved` ratchets that
absence. The production-source scan for `EvaluateDynamicExpressionProgram(` also
returns no matches, and
`SourceGate_E4_DynamicExpressionProgramBridge_IsCompletelyRemoved` ratchets that
absence. `BytecodeProofManifestTests` now also ratchets absence of the old
binding-target bridge callers and definition. The E4 source inventory still
finds only `UnifiedBytecodeExpressionProgramExecutor.ExecuteDynamic` as an open
dynamic expression bridge; the broader E5 inventory still finds
`ExecutionPlanRunner.RunScript` and broad `ExecutionPlanRunner` entry points, so
runner-internal expression evaluation remains classified under E5 rather than
blocking this finite E4 rebaseline.

## Known soft spots
1. **Named compiler-decline leaves** (A51a1/A51a2, A51b-A51k, A51m, and B47a) are now source-inventoried in the expansion contract; future `TryCompile` reason drift must update that contract and the focused source gate.
2. **Resumable suspension machinery** (B36 plus remaining B24 class-definition state) — async iterator drivers, super property reads/writes/updates, try/catch/finally, driver cleanup, nested function literals, simple nested class declarations including plain `extends` declarations with activation-safe explicit public derived constructors, public non-computed instance methods, public instance super members/fields, public static super members, public non-computed static fields and mixes with public static methods/accessors, static-block-only class declarations with activation-capturing closures, and delegated async-generator `yield*` are already admitted; remaining resumable rows are narrower declaration/class-definition ownership problems.

## Finite Open-Row Inventory (2026-06-07)

This table is the current ledger for the remaining open rows. It does not close
any row by itself; it names the source owner, proof anchor, and residue boundary
future slices must preserve before changing row status.

Ordinary work parking guardrail: A1/A2 and closed D1/D2/D3 are not parking
buckets. Keep call/member/super/private call boundaries in A51g, the remaining
A51 compiler leaves in their A51 rows, B24h/B36 class/static-block work in
B24h/B36, E4 expression-program retirement in E4, and E5 runner retirement in
E5b-E5d while terminal dynamic residue stays in E5e only as an exclusion boundary.

| Row | Owner source surface | Proof anchor family | Residue classification |
|---|---|---|---|
| A1 | `UnifiedBytecodeProductionEligibility`, `UnifiedBytecodeResumableActivation`, dynamic-identifier VM op handling, and captured-closure invocation bridges | Manifest `A1-captured-closure-routes` plus `A1-activation-residue-umbrella-stays-open-through-direct-eval` | Mixed: admitted captured-closure work is non-residue; the remaining activation umbrella points at A2/D3 dynamic residue and must stay open until those dynamic lanes are owned |
| A2 | `UnifiedBytecodeProductionEligibility`, direct-eval call-boundary checks, `TypedAstEvaluator.UnifiedBytecodeResumableActivation`, and live-`with` closure-chain routing | Manifest `A2-sync-declaration-free-direct-eval-routes`, `A2-sync-bounded-arguments-direct-eval-routes`, `A2-resumable-generator-declaration-free-direct-eval-routes`, `A2-resumable-async-declaration-free-direct-eval-routes`, `A2-resumable-async-generator-declaration-free-direct-eval-settles`, the three resumable arguments-direct-eval source-presence rows, `A2-resumable-dynamic-activation-direct-eval-boundary-stays-open`, and the A2 direct-eval/retained-with decline rows | Mixed: sync declaration-free single-literal direct eval, sync bounded caller-activation/implicit-arguments reads, and resumable generator/async/async-generator declaration-free literal eval are admitted; declaration-bearing eval, runtime-source eval, multi-arg/spread eval, retained live-`with` closure chains, broader arguments-dependent eval, and unowned dynamic activation parity remain dynamic residue or open activation work |
| A51a1 | `UnifiedBytecodeCompiler.TryCompile` and production-eligibility malformed entrypoint/target integrity guards | Manifest `A51a1-malformed-entrypoint-and-invalid-target-guards-stay-defensive` | Defensive malformed/manual-plan protection; not dynamic residue and not a generated-source fallback |
| A51a2 | `UnifiedBytecodeCompiler.TryCompile` loop-control and statement-topology diagnostics plus the expansion-contract reason-template inventory | Manifest `A51a2-loop-control-topology-gate-stays-open` | Ordinary non-residue compiler topology work; generated-code loop-control gaps stay open until topology and cleanup are VM-owned |
| A51b | `UnifiedBytecodeCompiler.TryCompile`, slot-layout metadata, flat-storage lowering, declaration/assignment/update target compilation, and iterator/for-in TDZ-head flat-slot mapping | Manifest `A51b-*stays-open` source-presence anchors for activation-slot metadata, flat-storage declaration/assignment/update target guards, and `A51b-iterator-for-in-tdz-head-flat-slot-mapping-stays-open`, plus the expansion-contract reason-template inventory | Ordinary non-residue storage/layout admission work |
| A51c | `UnifiedBytecodeCompiler.TryCompile`, non-simple/destructuring catch binding topology, unsupported catch binding slot mapping, catch/lexical dynamic declaration lowering, non-flat scope/environment TDZ-head binding storage, and active-`with` dynamic-name checks | Manifest `A51c-*stays-open` source-presence anchors for non-simple/destructuring catch binding topology, unsupported catch binding slot mapping, lexical dynamic declaration storage, active-with dynamic-name gates, materialized-activation dynamic residue, and the ordinary dynamic-name activation-slot boundary, plus the expansion-contract reason-template inventory | Mixed: catch/TDZ storage that depends on scope/environment ownership is ordinary non-residue work; active-`with` dynamic-name ownership remains dynamic residue |
| A51f3 | Expression-program dynamic-name lowering in `UnifiedBytecodeCompiler`, ordinary dynamic-name dependency gating in `UnifiedBytecodeProductionEligibility`, and dynamic reference/update/delete VM ops in `UnifiedBytecodeVirtualMachine` | Manifest `A51f3-zero-depth-catch-free-store-routes` plus `A51f3-dynamic-assignment-reference-gate-stays-open` | Mixed: zero-depth catch/finally reads, `typeof`, and plain stores are non-residue admitted route-enabling work; zero-depth catch/finally consumed assignment references, updates, compound/logical writes, and deletes remain open as route-enabling evidence even though the ordinary expression loop owns those dynamic ops after the route is enabled. Lexical dynamic declarations, active-`with`, and materialized activation ownership remain open |
| A51f5 | Expression-program private-neighbor lowering in `UnifiedBytecodeCompiler` and private-name-aware property/call VM helpers | Manifest `A51f5-*` private-read, optional-private, receiver-prefix call/write/update rows plus private-neighbor open source-presence rows | Ordinary non-residue private-adjacent compiler work; private delete-defense/manual-plan diagnostics remain defensive open anchors |
| A51g1-A51g4 | Call-target preparation, direct-eval boundary metadata, member/super/private call-target preparation, and nested invocation-boundary compilation in `UnifiedBytecodeCompiler`, `UnifiedBytecodeProductionEligibility`, and call/super handlers in `UnifiedBytecodeVirtualMachine` | Existing focused anchors: direct-eval route/decline tests, private call-target admission tests, super call-target preparation tests, `TryAppendFirstBoundaryCallTargetPreparation`, `TryIsFirstBoundaryCallTargetPreparationCandidate`, `TryAppendAdmittedComplexCallArgumentRegion`, and `TryValidateAdmittedComplexCallArgumentRegion`; no manifest closure row yet | Mixed: ordinary member/super/private call-target and invocation-boundary work is non-residue; multi-arg/spread/runtime-source/declaration-bearing direct eval and `Function(...)` produced bodies remain D1/D4 dynamic residue unless a future issue explicitly owns them |
| A51h | Literal/span lowering in `UnifiedBytecodeCompiler` for method/accessor/private/name-inference restrictions plus logical-control and conditional consequent/alternate operands in simple literal spans | Manifest `A51h-*stays-open` source-presence anchors for non-container literal/span reason templates plus the expansion-contract inventory | Ordinary non-residue literal/span admission work |
| A51h1 | Literal/span lowering in `UnifiedBytecodeCompiler` for array/object spread source spans and computed object key spans | Manifest `A51h1-*stays-open` source-presence anchors for spread-source and computed-object-key reason templates plus the expansion-contract inventory | Ordinary non-residue literal/span admission work; call/private/dynamic-key sources remain excluded unless already admitted by the measured span helpers |
| A51h2 | Literal-container span lowering in `UnifiedBytecodeCompiler` and matching measurement in `UnifiedBytecodeProductionEligibility` for array/object/template literals | Manifest `A51h2-*stays-open` source-presence anchors for literal-container appender/measurer helpers plus array/template boundary diagnostics | Ordinary non-residue literal/span admission work |
| A51i | Property-read lowering for computed, optional, private, and receiver-boundary shapes in `UnifiedBytecodeCompiler` and property-read VM helpers | Source-presence/source-ratchet proof: A51i property-read reason templates in the expansion contract; no manifest closure row yet | Ordinary non-residue property-read admission work |
| A51j | Property write, compound/logical write, update, delete, name-inference, key-span, and RHS-span lowering | Manifest `A51j-logical-property-write-cleanup-start-gate-stays-open` plus A51j property-mutation reason templates in the expansion contract | Ordinary non-residue mutation admission work |
| A51k | Expression-span lowering for open unsupported simple binary/operator, unsupported simple-operand, and call/private/dynamic-neighbor diagnostics, excluding conditional consequent/alternate simple-literal-span diagnostics now tracked by A51h and keeping call/private/dynamic-neighbor operands outside A51k1 | Manifest `A51k-*stays-open` source-presence anchors for simple binary/operator, simple-operand, and neighbor-boundary reason templates plus the expansion-contract inventory | Ordinary non-residue operand-span admission work |
| A51k1 | Expression-span lowering for simple binary/unary/control/conditional operands that compose already-admitted literal-value spans, mirrored by `UnifiedBytecodeProductionEligibility` simple value operand-span walkers | Manifest `A51k1-*present` source-presence anchors for compiler append helpers and eligibility measurers plus the expansion-contract inventory | Closed inventory/proof rebaseline for already-admitted simple value operand spans; call/private/dynamic-neighbor operands remain outside this child |
| B24h | Class-expression `LoadClassLiteral` admission, computed member-name programs, class static-block production routing, and class-definition environment bridging in `ClassDefinitionExtensions` | Manifest `B24h-*` computed-name, static-block, direct-eval-literal, and runtime-source direct-eval rows | Mixed: admitted computed/static-block slices are non-residue; runtime-source direct eval is dynamic residue, while non-production static-block plans and the broader class-definition environment bridge are ordinary class-definition fallback work |
| B36 | Resumable declaration pre-population, `DeclareFunction`/`DeclareClass` lowering, class static-block production routing, and resumable body-environment materialization | Manifest `B36-*` helper/class-declaration/static-block rows, including exact open rows for dynamic eval helpers, arguments eval helpers, helper direct-eval cache decline, synthetic activation-capture decline, runtime-source direct eval, non-production static-block plans, static-block IR fallback, static-field shape guards, deferred class-definition environment bridging, static-member shape guards, computed-member activation captures, and computed-field activation captures | Mixed: dynamic/eval helpers and runtime-source direct eval are dynamic residue; static-field/static-member shape guards, computed-member/field activation captures, deferred class-definition environment bridging, and non-production static-block plans are ordinary class-definition fallback work |
| E4 | `UnifiedBytecodeExpressionProgramExecutor.ExecuteDynamic`, deleted `EvaluateStandaloneExpressionProgram` / `EvaluateLoweredExpressionProgram` / `EvaluateDynamicExpressionProgram` bridges, binding-target helper entrypoints, and `ProfileRunner` expression-program profile path | Manifest `E4-*` source-absence/source-presence rows | Finite source-retirement inventory for the last dynamic expression-program bridge; deleted bridge names stay as source-absence ratchets, standalone helper call sites stay allowlisted, and runner-internal expression evaluation belongs to E5 |
| E5a | Eligible class static-block body routing before fallback | Manifest `E5-static-block-body-routes-when-eligible` | Admitted non-residue route; kept separate from open static-block `ExecutionPlanRunner.RunScript` fallback retirement |
| E5b | Classified `ExecutionPlanRunner` construction and remaining `RunScript` / `RunSync` / `ExecuteAsyncStep` call sites | Manifest `E5-ir-runner-*still-present` source-allowlist rows | Ordinary runner-entrypoint retirement work after A/B/C parity; broad Core type/method presence is not itself a hot-path blocker |
| E5c | Classified script fallback plus explicit declined static-block `RunScript` residue | Manifest `E5-script-ir-fallback-still-runs-execution-plan-runner` and `E5-static-block-ir-fallback-still-runs-execution-plan-runner` | Ordinary script fallback retirement work remains in E5c; declined static-block plans stay as explicit classified static-block residue outside ordinary script fallback retirement, and admitted static-block production routing belongs to E5a |
| E5d | Classified sync function, async function, and sync generator declined-body runner fallbacks | Manifest `E5-function-runner-fallback-still-constructs-runner`, `E5-sync-generator-declined-body-runner-still-present`, and `E5-async-function-ir-step-runner-still-present` | Ordinary function/resumable fallback retirement work after production bytecode, simple expression routes, trampolines, and constructor/class invocation bridges decline |
| E5e | Terminal dynamic residue exclusion boundary | Manifest `E5-terminal-dynamic-residue-excluded-from-ordinary-runner-retirement` | Multi-arg/spread/runtime-source/declaration direct eval, awaited-with object evaluation, retained live `with` scopes outside the VM current-environment lane, eval-injected bindings, and `Function(...)`-produced bodies remain terminal dynamic residue outside ordinary E5 fallback retirement |

---

_Checklist status: 137 / 161 complete. The latest row-accounted recount keeps A1/A2, the remaining A51 compiler leaves including A51g1-A51g4 call/member/super/private call-boundary children and A51h/A51h1/A51h2 literal/span rows, B24h/B36 class/static-block work, E4 expression-program retirement, and E5b-E5e runner retirement/exclusion rows visible as the source-of-truth remaining gaps while E5a separately records admitted static-block routing and E5c keeps the static-block fallback as explicit declined residue rather than ordinary script fallback retirement. A7 is closed for constructor/private-name activation, A51a1 is closed as malformed/manual-plan protection, A51a2 remains open for generated loop-control topology, A51c now carries explicit source-presence anchors for its catch-binding topology, catch-binding slot mapping, lexical dynamic declaration, active-with dynamic-name, materialized-activation dynamic residue, and ordinary dynamic-name activation-slot guards, A51f3 has narrowed for zero-depth catch/finally reads, `typeof`, and plain statement free stores while keeping consumed assignment references, updates, compound/logical writes, and deletes open only as zero-depth exception-region route-enabling evidence, A51f5 has narrowed: plain private named reads used as nested expression operands, single-hop optional private reads, private receiver-prefix named calls, private receiver-prefix public named/computed writes, and one-hop private receiver-prefix public named/computed updates now route, and B24c class expressions now include pure public non-computed static-field function-literal closure initializers. A51g is now a decomposed parent with direct-eval metadata, private-adjacent call-target, member/super call-target, and nested invocation-boundary child rows; A51h keeps method/accessor, control-expression, and conditional simple-literal-span inventory, A51h1 keeps spread-source and computed-object-key literal-span inventory, A51h2 carries the array/object/template literal-container appender/measurer helper lane separately from A51h's non-container residue, and A51k1 now carries the simple value operand-span helper/walker lane separately from A51k's remaining diagnostics. D1/D4 terminal dynamic residue and A51f5/A51i/A51j private/property neighbors remain excluded. A51l is retired as a standalone diagnostics bucket and its former catch/try/driver cleanup templates map to A51a2/A51c/A51d/A51j plus explicit resumable eligibility guards. Private delete-defense, chained optional-private, compound/logical/private-terminal mutation-neighbor, deeper private-prefix update, and super-neighbor diagnostics remain. B24h stays open for computed-name activation delete/construct residue, runtime-source direct eval/non-production static-block plan ownership, and the class-definition environment bridge, and B36 stays partial with exact open manifest rows for dynamic eval helpers, arguments eval helpers, runtime-source direct eval, non-production static-block plans, static-block IR fallback, static-field shape guards, deferred class-definition environment bridging, static-member shape guards, computed-member activation captures, and computed-field activation captures instead of closing those rows. Phase A stands at 64 / 81 complete. Phase B stands at 55 / 57 complete. A+B+C+D stands at 127 / 146 complete; Phase E stands at 5 / 10 complete after the E5 child split; the D5 known-open non-residue ratchet count remains 0. The resumable opcode gap inventory is 5, and the instruction gap inventory is 0. `BytecodeProofManifestTests` now verifies the plan-proof manifest, including source-presence blockers for A51a2/A51c/A51h/A51h1/A51h2/A51j/E4/E5b-E5e, A51k open diagnostics, A51k1 simple value operand-span owner anchors, source-absence proof for E6, malformed-plan guard anchors for A51a1/A51d, general expression-loop opcode coverage for A51f1, standalone-expression proof for A51f2, zero-depth catch/finally dynamic-name store admission plus remaining catch-only dynamic assignment-reference trigger proof for A51f3, implicit-arguments expression-loop closure for A51f4, A2 sync declaration-free direct-eval admission, A2 sync bounded-arguments direct-eval admission, A2 resumable generator/async/async-generator declaration-free literal direct-eval admission and generator/async/async-generator arguments-direct-eval open blockers, A2 retained live-`with` closure residue, A7 private-name constructor admission, B24c static-field closure initializer admission, B24h activation-call/IIFE computed-name admission plus activation-delete/construct decline rows, B24h/B36 declaration-free literal direct-eval static-block admission plus runtime-source direct-eval declines, B36 source-presence rows for dynamic/arguments eval helpers, non-production static-block ownership, static-block IR fallback, static-field shape guards, deferred class-definition environment bridging, static-member shape guards, computed-member activation captures, and computed-field activation captures, D3 active-current-environment `with` admission plus awaited with-object residue wording, and B36 explicit derived-constructor, public instance/static super-member, public super-field, public non-computed instance-method, static-field declaration admission including non-extends mixed public static methods/accessors and non-extends closure-producing static fields, closure-producing static fields including super-dependent closures, closure-producing static blocks, and mixed public static-field/static-block declaration rows._
